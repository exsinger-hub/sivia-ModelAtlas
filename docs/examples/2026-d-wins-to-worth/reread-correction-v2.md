Correct the attached sports-team schematic while keeping its two-panel composition, equations, state tuples, censored-demand plot and concise labels. This is an endpoint repair, not a new model. Remove incorrect connections FIRST, then draw only the specified ones.
DELETE: the long teal route from total revenue R_k across panel a back to popularity; the Schedule→Popularity vertical arrow; both crossed vertical teal "To strength update"/"To popularity update" routes; the ΔS(a) branch into the cash equation; the Borrow/repay arrow into profit; the arrow from the optimizer down to the bottom rolling path; the arrowhead at the right end of the bottom rolling return. Also remove the repeated ΔS(a) label outside the increment vector and orphan arrow segments. No causal arrow may say revenue changes current popularity or borrowing creates profit.
CORRECT UPPER INPUTS: effective strength E_k+ΔE(a)→Elo→Pr(win_g). Schedule/pregame context and P_k+ΔP(a) separately enter x_g along their existing short rightward predictor routes. Nothing goes from Schedule into P_k. Keep x_g→censored demand, lognormal model and Â_g=E[min(d_g,K_g)|x_g].
CORRECT REVENUE ADDITION: Ticket revenue and Other revenue enter a small PLUS junction on separate short branches; that sum yields R_k. Do not draw ticket revenue serially through Other revenue. From R_k draw a single charcoal downward route, in the right gutter, ending exactly at R_k in the profit equation. It must not join any teal control route.
PRICE: keep occupancy→Pricing scenario and assumed price-response loop. Add a short link from the estimated attendance expression to the occupancy label Â_g/K_g. In the ticket-revenue sum, remove the stray hat over Σ; it is an ordinary summation Σ_g p_g A_g(p_g;ε).
CONTROL: current-state tuple→optimizer, optimizer→Roster action a_k, roster action→increment vector ΔE/ΔP/ΔS. Use a short labeled output port beside the vector for "ΔE → strength" and another "ΔP → popularity", and matching small teal input ports immediately beside the two upper-panel state expressions. Do not draw long crossing control cables between these ports. These explicitly named paired ports replace the faulty cross-panel arrows. The ΔS row has ONE short rightward connector labelled "Salary increment" ending at S_k in Π_k=R_k−S_k−O_k−I_k, not at cash or debt.
FINANCE: preserve I_k=r_kD_k. Borrow/repay feeds only the matching Borrow_k and Repay_k terms in the two stock equations, with a short bracket; it does NOT feed Π_k. Preserve Cash_{k+1}=Cash_k+Π_k+Borrow_k−Repay_k and D_{k+1}=D_k+Borrow_k−Repay_k. Preserve "Simplified cash book".
VALUATION: R_k and profit/cash-flow basis feed the three valuation methods through a small bracket labelled "Financial basis". V_k feeds D_target=ℓ_kV_k via a short thin connector. The optimizer's selected ℓ_k has a matching teal output/input port labelled "ℓ_k" at the target relation. Keep the existing precise note about §7.2 weighted aggregation versus later revenue multiples; no weights added.
EVALUATION: from Π_k and V_k, draw small labeled output ports "Evaluate actions" and match them at the optimizer input. This avoids several crossing return lines. The objective remains a choice of action and target leverage subject to cash/debt limits. Do not add point estimates or change the unusual source risk definition.
NEXT STATE: cash/debt update feeds the next-state tuple as already shown. Add a small local label at its E/P rows, "Updated games and demand", to explain their update. The BOTTOM ROLLING RETURN starts at the next-state tuple on the RIGHT, travels left along the lower margin, then ends with ONE arrowhead at the current-state tuple on the LEFT. No arrowhead pointing into the next-state tuple on that return. Label "Next segment: observe and re-optimize".
STYLE: erase pale rounded rectangle fills and borders behind states, action selector, increment vector, profit/cash equations, valuation list and optimizer. Keep square state brackets, true mathematical grouping, thin panel divider, the plot and constraint marks. Use white background and calm serif mathematical type. Keep existing text readable; no new module boxes. The result must be an inspectable state-transition model, not an attractive graph with inaccurate arrows.
