Edit ONLY the bottom-left Mixed-effects comparison region of the attached figure. Everything else must remain unchanged: inference geometry, gate equations, risk axes, open white layout, labels, colors and connectors.
The current support diagram misleadingly sends fixed effects only to judges and random effects only to fans. Remove those two separate arrows and the separated rows. Replace that region with this clean compact specification on white:
Heading: Mixed-effects comparison
First line: "Both outcomes: judges / inferred fans"
Second line: "y = β₀ + xᵀβ + α_season + u_pro + u_celebrity + ε"
Under the appropriate portions, a short brace marks xᵀβ + α_season as "fixed effects" and another marks u_pro + u_celebrity as "random intercepts". If braces would be cramped, use a small third line "Shared fixed + random-effects structure".
Last line: "Point estimates; uncertainty not propagated".
This single model structure is fitted separately for the two outcomes; it does not say they share equal coefficients. Use no arrows assigning one component to only one outcome. Do not add boxes. Keep text readable and within the original support area.
