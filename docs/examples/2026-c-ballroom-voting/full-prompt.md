# Complete ImageGen prompt record

## Call 1 · generate

Output: `overview-v1.png` · artifact `exec-1868dfcd-e20a-4848-adb5-4e3def5f43d1.png`.

Use case: infographic-diagram
Asset: English paper-to-overview illustration, landscape 1536 x 1024, intended180mm full-text width before assumptions.
Purpose: Explain "Behind the Ballroom Scores: Reconstructing Fan Voting and Designing Fairer Rules for DWTS". Hidden fan SHARE is inferred under a rule, then supports two parallel analyses and a redesigned weekly voting mechanism. Votes are not directly observed; never claim the true vote totals are recovered. Draw no empirical result values.

Composition: clean PURE WHITE (#FFFFFF) page with crisp navy sans-serif text, no dark background, no blurred glow, no shadows or vignette. Refined editorial, flat vector-like scientific illustration with muted teal, violet and warm amber accents. Main short title "Behind the ballroom scores". Organize into upper shared inference (45%), middle two parallel analysis scenes (30%), lower targeted rule design (25%). Meaningful ballroom, judge, fan and ballot motifs integrated with methods, no cluttered unrelated icons.

Top-left compact input scene titled "Observed weekly data": a small stylized dancing pair (no real people), beside a judge scorecard WITHOUT numbers, an elimination roster with neutral check/cross symbols. Exact input labels "Judge scores", "Eliminations", "Contestant traits". Small clean preprocessing label "Clean + normalize".

Top-right dominant region titled "Infer hidden fan support": two complementary side-by-side units, not a serial chain. Unit A "Feasible set" with a simple convex polygon schematic and short label "Share intervals"; Unit B "Plackett–Luce likelihood" with small abstract contestant probability tokens and short label "Point estimates". One shared bracket beneath: "Rule-conditional fan shares". A small nearby note "Not observed vote totals". Below, a narrow diagnostics band: "Feasibility · consistency · uncertainty". No fake confidence interval values, fake distributions, axis ticks or percentages. Connect cleaned judge scores and eliminations to BOTH units via clean splitter. Contestant traits remain available to the lower-right analysis.

Middle-left region title "Compare voting rules": three mini ballot cards labeled "Rank", "Percentage", "Judges' Save". Same judge scores and inferred shares feed all three through one input bar. Beneath, text "Same contestants · same week". Bottom label "Cross-inference audit". Use symbols and routes to convey alternative rule applications, NOT a sequence Rank->Percentage->Judges'Save. Avoid full-season replay imagery. Direct down-arrow from shared inference output into this region.

Middle-right region title "Separate contestant and partner effects": a dancer-pair glyph beside "Mixed-effects models", branching to two outcomes "Judges" and "Fans". Small two-line annotations "Traits + season effects" and "Pro + celebrity intercepts". Direct down-arrow from shared inference output into this region; an independent small input "Contestant traits" at its side. Do not draw an arrow from rule comparison into mixed-effects, because these are parallel analyses, not serial dependencies. Do not imply fully propagated inference uncertainty: use label "Fan point estimates" on the input to the Fans analysis if space permits.

Bottom central wide rule-design band title "MARS-Soft+Disagreement". Main three-step sequence "Rank-sum baseline" -> "Targeted soft protection" -> "Weekly elimination". Under the middle protection step place a legible AND gate text: "High judge rank + weaker fan rank". Show this as the necessary condition for protection, not universal rescue. Two evidence arrows merge from the middle analysis regions into this bottom region, each ending clearly at the band boundary.

Bottom-most slim separated evaluation strip: "Tune: seasons1–17" -> "Test: seasons18–34"; four short metric labels alongside: "Rescue", "Balance", "Fan impact", "Changes". These are source-defined train/test groups, not fabricated outcome numbers. One tiny footer: "Method overview · schematic".

Visual grammar: teal observed/inferred data, violet comparative analysis, blue hierarchical effects, warm amber proposed mechanism. Dark consistent connectors, no connector through words or dancer art; separate clean gutters. Short titles, readable text, ample padding. Use en dash in seasons only if legible. Do not place full paper title, sources/page numbers, awards, team ID, institution or watermark inside image.

Evidence authority (instructions ONLY, do not print these sections): PDFphysicalpp7–10 section4 feasible-set/likelihood and nonunique rule-conditional relative support; pp11–14 section5 week-level counterfactual Rank/Percentage/Save and cross-inference; pp14–18 section6 mixed-effects uses estimated fan shares as point inputs and does not model measurement error; pp18–20 section7 gated rank-based MARS and temporal split; pp21–22 sensitivity. This is a method map, not evidence of reproduced results or a full-season simulation.

Reference adaptation: inspected source Figure1 at PDFp5. Retain shared inference, fork into rule audit and trait effects, converge on redesign. Replace generic rounded-box inventories with meaningful scenes. Do not copy source image pixels or its ambiguous lateral arrow.
Final checks: feasible-set and likelihood complementary; fan shares not actual vote totals; rule comparison and mixed-effects parallel; judge-elite AND fan disagreement gate visible; weekly not season-long counterfactual; tuning earlier seasons, testing later; no generated quantitative results; white background and sharply legible typography.

## Call 2 · edit

Input: `overview-v1.png`.

Output: `overview.png` · artifact `exec-de79de9c-1d90-41d5-ac61-b0eced737788.png`.

Use case: precise-object-edit. Edit the supplied ballroom overview; keep scientific labels, icons, two complementary inference units, two middle analysis panels and bottom MARS mechanism. Fix only contrast and critical connector routing.
1. Replace ALL blurred, smoky, dark gradient background outside panels with solid pure white (#FFFFFF). No halos, no vignette, no dark areas. Main title "Behind the ballroom scores" is solid navy on pure white. Keep panel interiors very light. Match clean flat journal style.
2. The current long connector running from observed data to Compare voting rules is misleading. Remove that downward branch entirely. Observed data -> inference only, as one arrow that clearly terminates at the LEFT BORDER OF THE WHOLE "Infer hidden fan support" container (not at Feasible set alone). Both inference units share that input.
3. Draw a SINGLE output fork from the center bottom of the "Infer hidden fan support" region, at the "Rule-conditional fan shares" output level, through an unobstructed horizontal gutter between top and middle panels. LEFT branch ends at TOP CENTER BORDER of "Compare voting rules". RIGHT branch ends at TOP CENTER BORDER of "Separate contestant and partner effects". Route both branches in the gap; make no line through text or drawings. Remove the two old parallel downward arrows that currently feed only the right panel. Each middle panel gets exactly one inference input arrow.
4. On the right panel replace the small text "Fan point estimates" beside the Fans outcome with "Inferred fan signal". This is an outcome constructed from inferred shares, not directly observed votes.
Preserve the lower analysis-to-MARS convergence, conditional protection "High judge rank + weaker fan rank (AND)", rank-sum -> soft protection -> weekly elimination, temporal Tune1–17 / Test18–34 and four evaluation metrics. Preserve "Not observed vote totals", feasibility/consistency/uncertainty and cross-inference audit. No team ID, author name, badges or new numerical results. All method text must be clearly readable against white.

## Round 3 — mechanism-focused redesign

Exact submitted text. ImageGen edit; input: `overview-v2.png`; output: `overview-v3.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Borrow visual principles from the inspected Sivia examples without copying their layouts or subject matter. Useful principles are persistent entity identities, a visible before/after state, a dominant mechanism with asymmetric supporting material, and feedback attached to the state that is actually updated. Do not import the examples' language-model internals, reinforcement learning, benchmark claims, floating metrics or decorative dashboards. Some Sivia examples themselves contain many panels; that is not the principle being requested. The adaptation here is the density of scientific explanation inside a well-composed scene, not the number of rounded rectangles.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: BEHIND THE BALROOM SCORES — a source-grounded MCM 2026 C overview.

Scientific purpose: show how weekly judge observations and eliminations constrain unobserved relative fan support, how that reconstructed support feeds two parallel analyses, and how the resulting evidence informs a narrowly targeted rank-based protection rule. The submitted paper is Team 2627351, Behind the Ballroom Scores: Reconstructing Fan Voting and Designing Fairer Rules for DWTS. This is a redesign of its analysis overview, not new research or a claim to reproduce its empirical results. The source is a 34-page submitted PDF. Scientific authority lies in sections 3–8, particularly physical pages 7–10 for inference, 11–14 for rule comparisons, 14–18 for mixed effects and 18–20 for MARS. Do not print this provenance paragraph.

Specific aesthetic direction: create an open statistical mechanism drawing organized around a constrained fan-share simplex on the left and an enlarged selective rank correction on the right. The two objects should be visibly different in scale and form. Between them, a narrow matched-rule comparison and a smaller paired-outcome model share the same inferred input. Think of an annotated scientific working sheet, not a product dashboard. The viewer should see a constrained region, repeated contestant identities and a conditional subtraction before noticing the words inference, comparison or redesign. No dancer illustrations, judge avatars, stars, trophy, crowd pictograms or decorative voting phone.

Canvas plan in normalized coordinates: maintain roughly five percent white margins. Place the small heading "Inferring votes, revising the rule" at the upper left, approximately x=5%, y=5%. Under it, give the leftmost 25% of the usable width to observations and inference. The middle 28% holds a slim upper rule-comparison object and a lower, smaller statistical-outcome relation. The rightmost 40% is the focal rank-correction mechanism, beginning below the heading and occupying the broad middle-height area. Along the bottom, use one unobtrusive temporal evaluation line rather than a full-width footer panel. Do not draw three enclosing columns; let the mathematical objects have their own irregular edges.

Observation object: at approximately x=6–25%, y=16–32%, draw a small paper-like weekly record with no curled corner and no thick perimeter. Use only a top rule, a header rule and two light horizontal rules. Its three rows use contestant identity markers A, B and E; E is explicitly the observed eliminated contestant. Make these identifiers neutral letters, not actual named participants or a claim about a specific week. The column headings are "Judge score" and "Exit". The judge entries are symbolic J_A, J_B and J_E, not invented numerical scores. The first two Exit entries are small horizontal dashes; the E row has a modest cross. Place "One active week" above the rule and "E: observed exit" beside the last row, if needed to clarify the cross.

Keep A as an open small circle, B as an open small triangle and E as an open small square beside their row letters. The letters remain the primary identity coding. Reuse these same shapes where the three-way explanatory simplex is annotated. They are illustrative identities that explain the general n-contestant calculation, not a reduction of the full model to exactly three real contestants. Do not add a numerical season or fabricated week index. The reference image's many anonymous contestant icons must be removed; the meaningful table is sufficient context.

From the observed record, draw a short solid arrow downward to a triangular probability simplex at x=7–28%, y=41–65%. The triangle is a mathematical domain, not an outer panel. Its vertices correspond to illustrative relative shares for A, B and E. Write "p ≥ 0, Σp = 1" just below it. Mark one softly tinted polygon inside the triangle as the feasible region, bounded by clean thin teal segments. Keep the surrounding infeasible area white with at most a few very light diagonal strokes; no dense hatching. The region's coordinates are deliberately schematic, so do not include tick marks, numeric vertex values or a fitted contour.

Adjacent to the constrained polygon print the short condition "C_E ≤ C_survivors". This is a consistency constraint for the observed eliminated contestant under a combining rule, not a claim that low raw fan rank alone determines exit. Define the score immediately above the simplex in a compact two-part equation: "C_i = w j_i + (1 − w)p_i". Here j_i is normalized judge support and p_i is relative fan support. Use lower-case j in the equation, distinguishing it from raw J_i in the record. A tiny note "normalized judges" can sit between raw scores and this combination, without introducing a separate softmax process box. There is no need to draw the exact softmax formula.

Inside the feasible region, add one hollow teal marker labeled "representative". Next to the triangle, show a short vertical bracket connected to a thin horizontal interval mark labeled "share bounds". This refers to the feasible-set interval output, not a confidence interval generated by a probability distribution. The marker and interval are alternative summaries of the same constrained support. A small second open amber point, outside the region if the schematic placement requires but preferably in it, is not acceptable because that would confuse estimator disagreement with feasibility; instead represent the complementary likelihood estimate in a separate, very small symbol below the triangle.

Specifically, below the triangle at x=8–27%, y=72–78%, write "Plackett–Luce" above a restrained likelihood-to-point glyph: three short neutral candidate ticks feeding one outlined teal point. Do not draw a bell curve or a probability histogram. Add "point estimate" beside that point. Give the likelihood glyph its own thin input from the weekly record, routed down the far-left margin, so it is clear that the feasible-set route and likelihood route are complementary methods receiving the same observations. Do not connect the triangle output into Plackett–Luce as if one method is a step inside the other.

Both inference summaries meet a small open junction near x=30%, y=49%, labeled "Rule-conditional fan shares". Below it, in smaller plain text, place "not observed vote totals". The diagram should not suggest that the paper recovered the exact number of votes or has a uniquely identified truth. Use a single solid teal spine extending toward x=35%, then fork upward and downward once. The upper branch enters the weekly-rule comparison; the lower branch enters the fan outcome of the mixed-effects relation. A separate thin gray input from the original judge-record area can enter the judge outcome along a quiet upper route. This prevents an implication that judge scores themselves were inferred from fans.

Weekly-rule comparison at x=36–57%, y=22–43%: draw a compact aligned three-row rule sheet, not three cards. Give it only one subtle header rule. The left labels are "Rank", "Percentage", and "Judges' Save". The right side is a set of three identically aligned neutral contestant markers in each row, signifying that the candidate set is held fixed. Do not show which named contestant wins or exits, since no empirical weekly result is supplied. Use a small brace over these aligned rows labeled "same week · same contestants". The distinctive operation of each row is conveyed by a tiny equation or operand glyph in the middle, not by a new illustration for each row.

For Rank, write "R_J + R_F". For Percentage, write "w j + (1 − w)p". For Judges' Save, write "bottom two → judge choice". The bottom-two phrase refers to the percentage-based shortlist in the source. Do not draw a general direct judge veto on any contestant. The rule sheet should be visibly an alternative-operator comparison on matched inputs. It is not a timeline, so there must be no arrow from Rank to Percentage to Judges' Save. A slim vertical alignment guide may visually keep the rows matched, but do not imply shared outcome values.

The comparison produces a short open-ended bracket or small arrow labeled "weekly disagreement" at its right edge. This is a diagnostic concept, not a numeric discrepancy count. It points through a dashed design-guidance route toward the left edge of the selective rank-correction mechanism. No actual bar chart, checkmark claiming superiority, winner medal, percentage improvement or green success stamp should be drawn. The source analysis is weekly, so do not imply a complete season replay or changed downstream contestant histories.

Mixed-effects relation at x=36–57%, y=57–77%: use two narrow aligned outcome lines labeled "Judge outcome" and "Inferred fan outcome". Each line begins with a small neutral response dot. On the right of both dots, a shared compact brace connects to the labels "traits + season" and "pro / celebrity". Distinguish the fixed covariate bundle from random-intercept grouping through a straight brace versus a double short vertical bracket, or another minimal grammar. There is no need to write a whole mixed-model equation because the spatial relation and the labels convey the intended overview. Do not use person silhouettes for each trait.

Under the outcome pair, add the short qualification "association, not causation". Fan estimates are point inputs here; uncertainty is not fully propagated in the paper's trait model. Do not draw confidence ribbons linking the feasible polygon to regression coefficients, and do not label the trait model uncertainty-aware. The intended comparison is how measured judges and inferred fans relate differently to observed characteristics while accounting for season and participant/pro grouping, not a claim that any demographic feature causes a voting outcome. Keep this lower branch smaller and lighter than the rank-correction mechanism.

A second dashed design-guidance connector begins at the mixed-effects brace and reaches the same design-guidance entry zone as the weekly-rule audit, but the two branches must remain independent. No solid arrow should connect the rule sheet to the mixed-effects relation or vice versa. If space is tight, the two dashed routes can end at a shared small label "design evidence" just before the focal mechanism. Do not make that label a box or a new integrator. A person following the arrows must be able to tell that evidence informs a designed rule rather than mechanically producing fitted MARS parameters.

Focal mechanism at x=63–95%, y=18–80%: this is the main visual event and should take at least roughly 35–40% of the ink-bearing area. Title it "Selective rank protection", modestly set, with "MARS" as a small adjacent identifier rather than another header strip. Begin with two clearly labeled thin horizontal ranking rails, "Judge rank R_J" and "Fan rank R_F", at y approximately 29% and 39%. Mark the better-to-worse direction once with a small axis arrow and "1 = best". Do not put unrelated contestant portraits on the rails.

On the judge rail, show a short near-best portion lightly washed in teal and bounded by a small brace labeled "judge elite". This is the top α fraction of normalized judge rank, not a fixed number of protected contestants. The fan rail uses the same neutral stroke. Place the same highlighted illustrative marker X on both rails, with X nearer the best end on the judge rail and farther right on the fan rail. X is a symbolic disagreement case, not one of the A/B/E input records and not an empirical result. If adding a different marker risks suggesting measured positions, omit other marks; the matched X positions alone explain the operation.

Connect X's two positions with a thin bent guide that visually exposes the rank difference. Label this guide "fan rank worse". The fan rail should not become a second statistical distribution. Beneath the rails, put two small condition strokes meeting an AND junction: one from the judge-elite brace and one from the positive rank difference. Print the word "AND" in ordinary charcoal text beside the junction, not inside a glowing logic chip. This is essential: protection is nonzero only for a judge-elite contestant whose fan rank is worse.

Directly below this junction, show the scalar gate and disagreement operand in two compact equations: "q = (R_J − 1)/(n − 1)" and "d = max(0, R_F − R_J)/(n − 1)". Place q nearer the judge rail and d nearer the fan-disagreement guide. Beneath q, write "g = (1 − q/α)_+^p". Here the plus subscript means positive part; render it as a genuine mathematical subscript, not as an added positive p or a plus sign outside the parentheses. If necessary, spell out a tiny adjacent label "soft gate" to clarify g, but do not append a long definition.

Separate the exponent p in the gate from the fan-share p_i used in inference by keeping these expressions in different local contexts. Do not rename the paper's exponent merely to make the drawing easier. Use the local "soft gate" label and the subscripted p_i elsewhere so the notation remains interpretable. Do not accidentally write the exponent as a subscript or write g = 1 − q/α without its positive part. The conditional gate must go to zero outside the elite range.

At y approximately 65%, create a single restrained mathematical transformation with a left baseline, a visible subtracted quantity and a right output: "S₀ = R_J + R_F" followed by "S = S₀ − λ_t g d". The subtraction is the key operation. The correction is not a positive risk penalty, nor an unconditional bonus applied to every contestant. Use a small teal underbrace beneath "λ_t g d" labeled "targeted reduction". Place "λ_t = βn" in small but readable type underneath, connected only to that correction term. Keep the correction term slightly darker than the surrounding formula.

To make the before/after visible without inventing a numerical result, draw two short aligned symbolic risk segments beneath the expression. Label them "baseline" and "protected case". The protected-case endpoint is shifted left by a short segment marked "λ_t g d"; there are no ticks or numeric lengths. A separate thin gray bypass from the AND junction goes directly to a third short label "otherwise S = S₀". Do not require three large panels for these states. The bypass is a mathematical alternative, not a failure path deserving a red cross.

At the rightmost edge of this mechanism, add the compact operation "Eliminate argmax S". An arrow from the corrected score formula ends at this operation; it must not emerge from the lower-risk illustrative segment as if the protected contestant is automatically eliminated. Make the text clear that larger S means worse combined standing. Beneath it, a very small note "deterministic ties" is optional if space permits. Do not change argmax to argmin and do not label the risk score a probability. The rule compares all current contestants; X merely demonstrates how one eligible contestant's score changes.

Temporal evaluation is a thin line at x=38–93%, y approximately 89%. Divide it once into an earlier segment and a later segment. Labels are "Tune: seasons 1–17" and "Test: seasons 18–34". Use a small vertical break and an arrow pointing from earlier to later time. A dashed connector from the MARS mechanism descends to this line, identifying evaluation of the designed rule. Do not feed test seasons back into tuning. Do not append success rates or imply that inferred elimination consistency is out-of-sample prediction.

Place one small note near the bottom-left margin: "Schematic · no empirical results shown". The figure contains illustrative mathematical geometry and symbolic contestant identities; it does not reproduce the paper's fitted values or prove that the revised rule is universally fairer. Do not include a statement such as more accurate, validated superior, unbiased or optimal fairness. The purpose is to expose the specific logic of reconstruction and targeted rule design, not to advertise results.

Detailed routing priorities: the raw-data arrow into the simplex should be short and vertical; the raw-data route into Plackett–Luce hugs the left boundary without touching the simplex vertices; the combined inference spine forks near x=32%; the upper branch approaches the matched-rule sheet from its left edge; the lower branch approaches only the fan outcome dot. The judge-outcome input may use a light gray elbow above the sheet and descend into its own dot, but it must not cut across the focal ranking rails. Design-evidence dashed lines approach the focal mechanism from its lower-left whitespace, not through the equations.

Keep at least one text-height between the simplex labels and its boundary, between the rule-sheet rows, between the ranking rails and their words, and between the score equation and its illustrative segments. Do not stack the soft-gate expressions into a dense wall of mathematics. Use each expression immediately beside the operand it defines, with enough white space to see which rank enters which function. Every formula requested is scientifically meaningful; no extra integral sign, neural-network node or random Greek letter belongs in unused space.

Object-scale priorities: the simplex should be large enough for the feasible polygon and one representative point to remain distinct at print width; the weekly record is smaller but legible; the rule sheet is a slim explanatory bridge; the mixed-effects model is an unobtrusive pair; the rank rails, AND condition and actual subtraction dominate. If a size compromise is necessary, simplify the neutral contestant markers in the rule sheet before reducing the focal math or shrinking the warning that votes are inferred. The graph is open and asymmetric, with the mathematical objects themselves defining its silhouette.

Palette: teal #327F87 means inferred support or the targeted protection operation; warm muted amber #B88745 marks the illustrative disagreement only; charcoal #293137 means observed records, ranks and text; gray #ADB2B3 means comparison structure or a bypass. Keep all broad spaces white. Do not alternate five unrelated colors across the five named activities. A lightly tinted feasible polygon and a narrow elite interval are enough fill for this entire image. The amber highlight must follow X's rank discrepancy, not become a new background panel.

Reference adaptation: use Sivia's visible before/after identity and conditional local change to make the gate understandable, and its use of richer internal state to replace stock icons. Do not copy a latent-language-workspace layout, an agent feedback loop or benchmark metric chips. This is an MCM voting paper, so the identity is a contestant's rank, the state is a constrained share or score, and the local change is a conditional subtraction. The final composition should not look interchangeable with a policy, ecology or engineering overview.

Final content check before rendering: the inferred fan share is relative, complementary estimators share observations, two analyses branch in parallel, counterfactual comparisons hold the week and active set fixed, trait associations are not causal, the correction requires both conditions, g uses the positive part, d cannot be negative, λ_t depends on n, lower risk is a reduction and elimination uses argmax. Earlier and later seasons remain separated. No numerical measurement, vote total, actual participant result or performance claim has been invented. Preserve this scientific specificity while making the page visually quiet.


## Round 4 — inspected correction

Exact submitted text. ImageGen edit; input: `overview-v3.png`; output: `overview-v4.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: BEHIND THE BALROOM SCORES — a source-grounded MCM 2026 C overview.

Scientific purpose: show how weekly judge observations and eliminations constrain unobserved relative fan support, how that reconstructed support feeds two parallel analyses, and how the resulting evidence informs a narrowly targeted rank-based protection rule. The submitted paper is Team 2627351, Behind the Ballroom Scores: Reconstructing Fan Voting and Designing Fairer Rules for DWTS. This is a redesign of its analysis overview, not new research or a claim to reproduce its empirical results. The source is a 34-page submitted PDF. Scientific authority lies in sections 3–8, particularly physical pages 7–10 for inference, 11–14 for rule comparisons, 14–18 for mixed effects and 18–20 for MARS. Do not print this provenance paragraph.

Specific aesthetic direction: create an open statistical mechanism drawing organized around a constrained fan-share simplex on the left and an enlarged selective rank correction on the right. The two objects should be visibly different in scale and form. Between them, a narrow matched-rule comparison and a smaller paired-outcome model share the same inferred input. Think of an annotated scientific working sheet, not a product dashboard. The viewer should see a constrained region, repeated contestant identities and a conditional subtraction before noticing the words inference, comparison or redesign. No dancer illustrations, judge avatars, stars, trophy, crowd pictograms or decorative voting phone.

Canvas plan in normalized coordinates: maintain roughly five percent white margins. Place the small heading "Inferring votes, revising the rule" at the upper left, approximately x=5%, y=5%. Under it, give the leftmost 25% of the usable width to observations and inference. The middle 28% holds a slim upper rule-comparison object and a lower, smaller statistical-outcome relation. The rightmost 40% is the focal rank-correction mechanism, beginning below the heading and occupying the broad middle-height area. Along the bottom, use one unobtrusive temporal evaluation line rather than a full-width footer panel. Do not draw three enclosing columns; let the mathematical objects have their own irregular edges.

Observation object: at approximately x=6–25%, y=16–32%, draw a small paper-like weekly record with no curled corner and no thick perimeter. Use only a top rule, a header rule and two light horizontal rules. Its three rows use contestant identity markers A, B and E; E is explicitly the observed eliminated contestant. Make these identifiers neutral letters, not actual named participants or a claim about a specific week. The column headings are "Judge score" and "Exit". The judge entries are symbolic J_A, J_B and J_E, not invented numerical scores. The first two Exit entries are small horizontal dashes; the E row has a modest cross. Place "One active week" above the rule and "E: observed exit" beside the last row, if needed to clarify the cross.

Keep A as an open small circle, B as an open small triangle and E as an open small square beside their row letters. The letters remain the primary identity coding. Reuse these same shapes where the three-way explanatory simplex is annotated. They are illustrative identities that explain the general n-contestant calculation, not a reduction of the full model to exactly three real contestants. Do not add a numerical season or fabricated week index. The reference image's many anonymous contestant icons must be removed; the meaningful table is sufficient context.

From the observed record, draw a short solid arrow downward to a triangular probability simplex at x=7–28%, y=41–65%. The triangle is a mathematical domain, not an outer panel. Its vertices correspond to illustrative relative shares for A, B and E. Write "p ≥ 0, Σp = 1" just below it. Mark one softly tinted polygon inside the triangle as the feasible region, bounded by clean thin teal segments. Keep the surrounding infeasible area white with at most a few very light diagonal strokes; no dense hatching. The region's coordinates are deliberately schematic, so do not include tick marks, numeric vertex values or a fitted contour.

Adjacent to the constrained polygon print the short condition "C_E ≤ C_survivors". This is a consistency constraint for the observed eliminated contestant under a combining rule, not a claim that low raw fan rank alone determines exit. Define the score immediately above the simplex in a compact two-part equation: "C_i = w j_i + (1 − w)p_i". Here j_i is normalized judge support and p_i is relative fan support. Use lower-case j in the equation, distinguishing it from raw J_i in the record. A tiny note "normalized judges" can sit between raw scores and this combination, without introducing a separate softmax process box. There is no need to draw the exact softmax formula.

Inside the feasible region, add one hollow teal marker labeled "representative". Next to the triangle, show a short vertical bracket connected to a thin horizontal interval mark labeled "share bounds". This refers to the feasible-set interval output, not a confidence interval generated by a probability distribution. The marker and interval are alternative summaries of the same constrained support. A small second open amber point, outside the region if the schematic placement requires but preferably in it, is not acceptable because that would confuse estimator disagreement with feasibility; instead represent the complementary likelihood estimate in a separate, very small symbol below the triangle.

Specifically, below the triangle at x=8–27%, y=72–78%, write "Plackett–Luce" above a restrained likelihood-to-point glyph: three short neutral candidate ticks feeding one outlined teal point. Do not draw a bell curve or a probability histogram. Add "point estimate" beside that point. Give the likelihood glyph its own thin input from the weekly record, routed down the far-left margin, so it is clear that the feasible-set route and likelihood route are complementary methods receiving the same observations. Do not connect the triangle output into Plackett–Luce as if one method is a step inside the other.

Both inference summaries meet a small open junction near x=30%, y=49%, labeled "Rule-conditional fan shares". Below it, in smaller plain text, place "not observed vote totals". The diagram should not suggest that the paper recovered the exact number of votes or has a uniquely identified truth. Use a single solid teal spine extending toward x=35%, then fork upward and downward once. The upper branch enters the weekly-rule comparison; the lower branch enters the fan outcome of the mixed-effects relation. A separate thin gray input from the original judge-record area can enter the judge outcome along a quiet upper route. This prevents an implication that judge scores themselves were inferred from fans.

Weekly-rule comparison at x=36–57%, y=22–43%: draw a compact aligned three-row rule sheet, not three cards. Give it only one subtle header rule. The left labels are "Rank", "Percentage", and "Judges' Save". The right side is a set of three identically aligned neutral contestant markers in each row, signifying that the candidate set is held fixed. Do not show which named contestant wins or exits, since no empirical weekly result is supplied. Use a small brace over these aligned rows labeled "same week · same contestants". The distinctive operation of each row is conveyed by a tiny equation or operand glyph in the middle, not by a new illustration for each row.

For Rank, write "R_J + R_F". For Percentage, write "w j + (1 − w)p". For Judges' Save, write "bottom two → judge choice". The bottom-two phrase refers to the percentage-based shortlist in the source. Do not draw a general direct judge veto on any contestant. The rule sheet should be visibly an alternative-operator comparison on matched inputs. It is not a timeline, so there must be no arrow from Rank to Percentage to Judges' Save. A slim vertical alignment guide may visually keep the rows matched, but do not imply shared outcome values.

The comparison produces a short open-ended bracket or small arrow labeled "weekly disagreement" at its right edge. This is a diagnostic concept, not a numeric discrepancy count. It points through a dashed design-guidance route toward the left edge of the selective rank-correction mechanism. No actual bar chart, checkmark claiming superiority, winner medal, percentage improvement or green success stamp should be drawn. The source analysis is weekly, so do not imply a complete season replay or changed downstream contestant histories.

Mixed-effects relation at x=36–57%, y=57–77%: use two narrow aligned outcome lines labeled "Judge outcome" and "Inferred fan outcome". Each line begins with a small neutral response dot. On the right of both dots, a shared compact brace connects to the labels "traits + season" and "pro / celebrity". Distinguish the fixed covariate bundle from random-intercept grouping through a straight brace versus a double short vertical bracket, or another minimal grammar. There is no need to write a whole mixed-model equation because the spatial relation and the labels convey the intended overview. Do not use person silhouettes for each trait.

Under the outcome pair, add the short qualification "association, not causation". Fan estimates are point inputs here; uncertainty is not fully propagated in the paper's trait model. Do not draw confidence ribbons linking the feasible polygon to regression coefficients, and do not label the trait model uncertainty-aware. The intended comparison is how measured judges and inferred fans relate differently to observed characteristics while accounting for season and participant/pro grouping, not a claim that any demographic feature causes a voting outcome. Keep this lower branch smaller and lighter than the rank-correction mechanism.

A second dashed design-guidance connector begins at the mixed-effects brace and reaches the same design-guidance entry zone as the weekly-rule audit, but the two branches must remain independent. No solid arrow should connect the rule sheet to the mixed-effects relation or vice versa. If space is tight, the two dashed routes can end at a shared small label "design evidence" just before the focal mechanism. Do not make that label a box or a new integrator. A person following the arrows must be able to tell that evidence informs a designed rule rather than mechanically producing fitted MARS parameters.

Focal mechanism at x=63–95%, y=18–80%: this is the main visual event and should take at least roughly 35–40% of the ink-bearing area. Title it "Selective rank protection", modestly set, with "MARS" as a small adjacent identifier rather than another header strip. Begin with two clearly labeled thin horizontal ranking rails, "Judge rank R_J" and "Fan rank R_F", at y approximately 29% and 39%. Mark the better-to-worse direction once with a small axis arrow and "1 = best". Do not put unrelated contestant portraits on the rails.

On the judge rail, show a short near-best portion lightly washed in teal and bounded by a small brace labeled "judge elite". This is the top α fraction of normalized judge rank, not a fixed number of protected contestants. The fan rail uses the same neutral stroke. Place the same highlighted illustrative marker X on both rails, with X nearer the best end on the judge rail and farther right on the fan rail. X is a symbolic disagreement case, not one of the A/B/E input records and not an empirical result. If adding a different marker risks suggesting measured positions, omit other marks; the matched X positions alone explain the operation.

Connect X's two positions with a thin bent guide that visually exposes the rank difference. Label this guide "fan rank worse". The fan rail should not become a second statistical distribution. Beneath the rails, put two small condition strokes meeting an AND junction: one from the judge-elite brace and one from the positive rank difference. Print the word "AND" in ordinary charcoal text beside the junction, not inside a glowing logic chip. This is essential: protection is nonzero only for a judge-elite contestant whose fan rank is worse.

Directly below this junction, show the scalar gate and disagreement operand in two compact equations: "q = (R_J − 1)/(n − 1)" and "d = max(0, R_F − R_J)/(n − 1)". Place q nearer the judge rail and d nearer the fan-disagreement guide. Beneath q, write "g = (1 − q/α)_+^p". Here the plus subscript means positive part; render it as a genuine mathematical subscript, not as an added positive p or a plus sign outside the parentheses. If necessary, spell out a tiny adjacent label "soft gate" to clarify g, but do not append a long definition.

Separate the exponent p in the gate from the fan-share p_i used in inference by keeping these expressions in different local contexts. Do not rename the paper's exponent merely to make the drawing easier. Use the local "soft gate" label and the subscripted p_i elsewhere so the notation remains interpretable. Do not accidentally write the exponent as a subscript or write g = 1 − q/α without its positive part. The conditional gate must go to zero outside the elite range.

At y approximately 65%, create a single restrained mathematical transformation with a left baseline, a visible subtracted quantity and a right output: "S₀ = R_J + R_F" followed by "S = S₀ − λ_t g d". The subtraction is the key operation. The correction is not a positive risk penalty, nor an unconditional bonus applied to every contestant. Use a small teal underbrace beneath "λ_t g d" labeled "targeted reduction". Place "λ_t = βn" in small but readable type underneath, connected only to that correction term. Keep the correction term slightly darker than the surrounding formula.

To make the before/after visible without inventing a numerical result, draw two short aligned symbolic risk segments beneath the expression. Label them "baseline" and "protected case". The protected-case endpoint is shifted left by a short segment marked "λ_t g d"; there are no ticks or numeric lengths. A separate thin gray bypass from the AND junction goes directly to a third short label "otherwise S = S₀". Do not require three large panels for these states. The bypass is a mathematical alternative, not a failure path deserving a red cross.

At the rightmost edge of this mechanism, add the compact operation "Eliminate argmax S". An arrow from the corrected score formula ends at this operation; it must not emerge from the lower-risk illustrative segment as if the protected contestant is automatically eliminated. Make the text clear that larger S means worse combined standing. Beneath it, a very small note "deterministic ties" is optional if space permits. Do not change argmax to argmin and do not label the risk score a probability. The rule compares all current contestants; X merely demonstrates how one eligible contestant's score changes.

Temporal evaluation is a thin line at x=38–93%, y approximately 89%. Divide it once into an earlier segment and a later segment. Labels are "Tune: seasons 1–17" and "Test: seasons 18–34". Use a small vertical break and an arrow pointing from earlier to later time. A dashed connector from the MARS mechanism descends to this line, identifying evaluation of the designed rule. Do not feed test seasons back into tuning. Do not append success rates or imply that inferred elimination consistency is out-of-sample prediction.

Place one small note near the bottom-left margin: "Schematic · no empirical results shown". The figure contains illustrative mathematical geometry and symbolic contestant identities; it does not reproduce the paper's fitted values or prove that the revised rule is universally fairer. Do not include a statement such as more accurate, validated superior, unbiased or optimal fairness. The purpose is to expose the specific logic of reconstruction and targeted rule design, not to advertise results.

Detailed routing priorities: the raw-data arrow into the simplex should be short and vertical; the raw-data route into Plackett–Luce hugs the left boundary without touching the simplex vertices; the combined inference spine forks near x=32%; the upper branch approaches the matched-rule sheet from its left edge; the lower branch approaches only the fan outcome dot. The judge-outcome input may use a light gray elbow above the sheet and descend into its own dot, but it must not cut across the focal ranking rails. Design-evidence dashed lines approach the focal mechanism from its lower-left whitespace, not through the equations.

Palette: teal #327F87 means inferred support or the targeted protection operation; warm muted amber #B88745 marks the illustrative disagreement only; charcoal #293137 means observed records, ranks and text; gray #ADB2B3 means comparison structure or a bypass. Keep all broad spaces white. Do not alternate five unrelated colors across the five named activities. A lightly tinted feasible polygon and a narrow elite interval are enough fill for this entire image. The amber highlight must follow X's rank discrepancy, not become a new background panel.

Final content check before rendering: the inferred fan share is relative, complementary estimators share observations, two analyses branch in parallel, counterfactual comparisons hold the week and active set fixed, trait associations are not causal, the correction requires both conditions, g uses the positive part, d cannot be negative, λ_t depends on n, lower risk is a reduction and elimination uses argmax. Earlier and later seasons remain separated. No numerical measurement, vote total, actual participant result or performance claim has been invented. Preserve this scientific specificity while making the page visually quiet.

MANDATORY CORRECTIONS TO THE FIRST REDESIGN: The supplied image is the first redesign, not the earlier dashboard. Keep its good table/simplex/matched-rule/conditional-formula concept, but fix these observed defects. First, remove the entire pale cyan rectangular background behind MARS; the right-side mathematical apparatus must sit on the same white page. Remove the bounding rectangle around the mixed-effects pair. Reduce the main figure heading by about one quarter and keep it left aligned. Keep all labels legible, but reduce repeated bold section headings.

Second, rebuild the data routes unambiguously. The raw judge-score record supplies the comparison sheet and the Judge outcome dot. One GRAY route must visibly start at the Judge score column, travel above the simplex and behind no labels, and fork to the comparison sheet and the Judge outcome dot. The TEAL Rule-conditional fan shares junction supplies the comparison sheet AND ONLY the Inferred fan outcome dot. Remove the current teal branch that ends at Judge outcome. There must never be a fan-inference arrow ending at the observed Judge outcome. Route the two outcome inputs into their respective dots from separate white corridors. If crowded, move the mixed-effects pair slightly down and left to make the two ports clear.

Third, the Plackett–Luce point estimate currently has no outgoing connection. Add a thin teal connector beginning at its point and ending at the SAME Rule-conditional fan shares junction as the feasible-set bounds. Move the point-estimate text downward slightly to make room. Its route should run in the white lane between the simplex and mixed-effects relation; do not connect it to the feasible polygon. Both estimators have their own original-data input and their own output into the shared inferred-share junction.

Fourth, remove the two baseline/protected-case miniature line-segment graphics, because their lengths currently misrepresent the subtracted portion. The two exact score expressions already display the before/after change. Keep S₀ = R_J + R_F and S = S₀ − λ_t g d, the targeted reduction underbrace, λ_t = βn and Eliminate argmax S. Place the bypass statement "otherwise S = S₀" beneath the equations, with a short, clearly conditional dashed route from the AND gate. There is no need for a made-up quantitative risk-length comparison.

Fifth, the two conditional input strokes should meet at ONE small AND junction before the correction expression, rather than merely terminating beside independent q and d labels. Keep q and d definitions near their respective rails, but visually join their condition strokes and lead that joined condition into the g d product. The short judge-elite interval must include illustrative marker X, not end ambiguously exactly at its center. Shift X slightly inside the elite interval, while retaining its clearly worse fan position.

Lastly, make the evaluation connector identify the whole Tune/Test timeline, not just the later test portion. It may terminate at the "Evaluate designed rule" label directly above the entire line. Keep the earlier/later split and do not feed test information back into tuning. Inspect these exact endpoint corrections before returning the revised image. Preserve all other source-grounded qualifications and do not introduce numeric outcomes.


## Round 5 — inspected correction

Exact submitted text. ImageGen edit; input: `overview-v4.png`; output: `overview.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: BEHIND THE BALROOM SCORES — a source-grounded MCM 2026 C overview.

Scientific purpose: show how weekly judge observations and eliminations constrain unobserved relative fan support, how that reconstructed support feeds two parallel analyses, and how the resulting evidence informs a narrowly targeted rank-based protection rule. The submitted paper is Team 2627351, Behind the Ballroom Scores: Reconstructing Fan Voting and Designing Fairer Rules for DWTS. This is a redesign of its analysis overview, not new research or a claim to reproduce its empirical results. The source is a 34-page submitted PDF. Scientific authority lies in sections 3–8, particularly physical pages 7–10 for inference, 11–14 for rule comparisons, 14–18 for mixed effects and 18–20 for MARS. Do not print this provenance paragraph.

Specific aesthetic direction: create an open statistical mechanism drawing organized around a constrained fan-share simplex on the left and an enlarged selective rank correction on the right. The two objects should be visibly different in scale and form. Between them, a narrow matched-rule comparison and a smaller paired-outcome model share the same inferred input. Think of an annotated scientific working sheet, not a product dashboard. The viewer should see a constrained region, repeated contestant identities and a conditional subtraction before noticing the words inference, comparison or redesign. No dancer illustrations, judge avatars, stars, trophy, crowd pictograms or decorative voting phone.

Canvas plan in normalized coordinates: maintain roughly five percent white margins. Place the small heading "Inferring votes, revising the rule" at the upper left, approximately x=5%, y=5%. Under it, give the leftmost 25% of the usable width to observations and inference. The middle 28% holds a slim upper rule-comparison object and a lower, smaller statistical-outcome relation. The rightmost 40% is the focal rank-correction mechanism, beginning below the heading and occupying the broad middle-height area. Along the bottom, use one unobtrusive temporal evaluation line rather than a full-width footer panel. Do not draw three enclosing columns; let the mathematical objects have their own irregular edges.

Observation object: at approximately x=6–25%, y=16–32%, draw a small paper-like weekly record with no curled corner and no thick perimeter. Use only a top rule, a header rule and two light horizontal rules. Its three rows use contestant identity markers A, B and E; E is explicitly the observed eliminated contestant. Make these identifiers neutral letters, not actual named participants or a claim about a specific week. The column headings are "Judge score" and "Exit". The judge entries are symbolic J_A, J_B and J_E, not invented numerical scores. The first two Exit entries are small horizontal dashes; the E row has a modest cross. Place "One active week" above the rule and "E: observed exit" beside the last row, if needed to clarify the cross.

Keep A as an open small circle, B as an open small triangle and E as an open small square beside their row letters. The letters remain the primary identity coding. Reuse these same shapes where the three-way explanatory simplex is annotated. They are illustrative identities that explain the general n-contestant calculation, not a reduction of the full model to exactly three real contestants. Do not add a numerical season or fabricated week index. The reference image's many anonymous contestant icons must be removed; the meaningful table is sufficient context.

From the observed record, draw a short solid arrow downward to a triangular probability simplex at x=7–28%, y=41–65%. The triangle is a mathematical domain, not an outer panel. Its vertices correspond to illustrative relative shares for A, B and E. Write "p ≥ 0, Σp = 1" just below it. Mark one softly tinted polygon inside the triangle as the feasible region, bounded by clean thin teal segments. Keep the surrounding infeasible area white with at most a few very light diagonal strokes; no dense hatching. The region's coordinates are deliberately schematic, so do not include tick marks, numeric vertex values or a fitted contour.

Adjacent to the constrained polygon print the short condition "C_E ≤ C_survivors". This is a consistency constraint for the observed eliminated contestant under a combining rule, not a claim that low raw fan rank alone determines exit. Define the score immediately above the simplex in a compact two-part equation: "C_i = w j_i + (1 − w)p_i". Here j_i is normalized judge support and p_i is relative fan support. Use lower-case j in the equation, distinguishing it from raw J_i in the record. A tiny note "normalized judges" can sit between raw scores and this combination, without introducing a separate softmax process box. There is no need to draw the exact softmax formula.

Inside the feasible region, add one hollow teal marker labeled "representative". Next to the triangle, show a short vertical bracket connected to a thin horizontal interval mark labeled "share bounds". This refers to the feasible-set interval output, not a confidence interval generated by a probability distribution. The marker and interval are alternative summaries of the same constrained support. A small second open amber point, outside the region if the schematic placement requires but preferably in it, is not acceptable because that would confuse estimator disagreement with feasibility; instead represent the complementary likelihood estimate in a separate, very small symbol below the triangle.

Specifically, below the triangle at x=8–27%, y=72–78%, write "Plackett–Luce" above a restrained likelihood-to-point glyph: three short neutral candidate ticks feeding one outlined teal point. Do not draw a bell curve or a probability histogram. Add "point estimate" beside that point. Give the likelihood glyph its own thin input from the weekly record, routed down the far-left margin, so it is clear that the feasible-set route and likelihood route are complementary methods receiving the same observations. Do not connect the triangle output into Plackett–Luce as if one method is a step inside the other.

Both inference summaries meet a small open junction near x=30%, y=49%, labeled "Rule-conditional fan shares". Below it, in smaller plain text, place "not observed vote totals". The diagram should not suggest that the paper recovered the exact number of votes or has a uniquely identified truth. Use a single solid teal spine extending toward x=35%, then fork upward and downward once. The upper branch enters the weekly-rule comparison; the lower branch enters the fan outcome of the mixed-effects relation. A separate thin gray input from the original judge-record area can enter the judge outcome along a quiet upper route. This prevents an implication that judge scores themselves were inferred from fans.

Weekly-rule comparison at x=36–57%, y=22–43%: draw a compact aligned three-row rule sheet, not three cards. Give it only one subtle header rule. The left labels are "Rank", "Percentage", and "Judges' Save". The right side is a set of three identically aligned neutral contestant markers in each row, signifying that the candidate set is held fixed. Do not show which named contestant wins or exits, since no empirical weekly result is supplied. Use a small brace over these aligned rows labeled "same week · same contestants". The distinctive operation of each row is conveyed by a tiny equation or operand glyph in the middle, not by a new illustration for each row.

For Rank, write "R_J + R_F". For Percentage, write "w j + (1 − w)p". For Judges' Save, write "bottom two → judge choice". The bottom-two phrase refers to the percentage-based shortlist in the source. Do not draw a general direct judge veto on any contestant. The rule sheet should be visibly an alternative-operator comparison on matched inputs. It is not a timeline, so there must be no arrow from Rank to Percentage to Judges' Save. A slim vertical alignment guide may visually keep the rows matched, but do not imply shared outcome values.

The comparison produces a short open-ended bracket or small arrow labeled "weekly disagreement" at its right edge. This is a diagnostic concept, not a numeric discrepancy count. It points through a dashed design-guidance route toward the left edge of the selective rank-correction mechanism. No actual bar chart, checkmark claiming superiority, winner medal, percentage improvement or green success stamp should be drawn. The source analysis is weekly, so do not imply a complete season replay or changed downstream contestant histories.

Mixed-effects relation at x=36–57%, y=57–77%: use two narrow aligned outcome lines labeled "Judge outcome" and "Inferred fan outcome". Each line begins with a small neutral response dot. On the right of both dots, a shared compact brace connects to the labels "traits + season" and "pro / celebrity". Distinguish the fixed covariate bundle from random-intercept grouping through a straight brace versus a double short vertical bracket, or another minimal grammar. There is no need to write a whole mixed-model equation because the spatial relation and the labels convey the intended overview. Do not use person silhouettes for each trait.

Under the outcome pair, add the short qualification "association, not causation". Fan estimates are point inputs here; uncertainty is not fully propagated in the paper's trait model. Do not draw confidence ribbons linking the feasible polygon to regression coefficients, and do not label the trait model uncertainty-aware. The intended comparison is how measured judges and inferred fans relate differently to observed characteristics while accounting for season and participant/pro grouping, not a claim that any demographic feature causes a voting outcome. Keep this lower branch smaller and lighter than the rank-correction mechanism.

A second dashed design-guidance connector begins at the mixed-effects brace and reaches the same design-guidance entry zone as the weekly-rule audit, but the two branches must remain independent. No solid arrow should connect the rule sheet to the mixed-effects relation or vice versa. If space is tight, the two dashed routes can end at a shared small label "design evidence" just before the focal mechanism. Do not make that label a box or a new integrator. A person following the arrows must be able to tell that evidence informs a designed rule rather than mechanically producing fitted MARS parameters.

Focal mechanism at x=63–95%, y=18–80%: this is the main visual event and should take at least roughly 35–40% of the ink-bearing area. Title it "Selective rank protection", modestly set, with "MARS" as a small adjacent identifier rather than another header strip. Begin with two clearly labeled thin horizontal ranking rails, "Judge rank R_J" and "Fan rank R_F", at y approximately 29% and 39%. Mark the better-to-worse direction once with a small axis arrow and "1 = best". Do not put unrelated contestant portraits on the rails.

On the judge rail, show a short near-best portion lightly washed in teal and bounded by a small brace labeled "judge elite". This is the top α fraction of normalized judge rank, not a fixed number of protected contestants. The fan rail uses the same neutral stroke. Place the same highlighted illustrative marker X on both rails, with X nearer the best end on the judge rail and farther right on the fan rail. X is a symbolic disagreement case, not one of the A/B/E input records and not an empirical result. If adding a different marker risks suggesting measured positions, omit other marks; the matched X positions alone explain the operation.

Connect X's two positions with a thin bent guide that visually exposes the rank difference. Label this guide "fan rank worse". The fan rail should not become a second statistical distribution. Beneath the rails, put two small condition strokes meeting an AND junction: one from the judge-elite brace and one from the positive rank difference. Print the word "AND" in ordinary charcoal text beside the junction, not inside a glowing logic chip. This is essential: protection is nonzero only for a judge-elite contestant whose fan rank is worse.

Directly below this junction, show the scalar gate and disagreement operand in two compact equations: "q = (R_J − 1)/(n − 1)" and "d = max(0, R_F − R_J)/(n − 1)". Place q nearer the judge rail and d nearer the fan-disagreement guide. Beneath q, write "g = (1 − q/α)_+^p". Here the plus subscript means positive part; render it as a genuine mathematical subscript, not as an added positive p or a plus sign outside the parentheses. If necessary, spell out a tiny adjacent label "soft gate" to clarify g, but do not append a long definition.

Separate the exponent p in the gate from the fan-share p_i used in inference by keeping these expressions in different local contexts. Do not rename the paper's exponent merely to make the drawing easier. Use the local "soft gate" label and the subscripted p_i elsewhere so the notation remains interpretable. Do not accidentally write the exponent as a subscript or write g = 1 − q/α without its positive part. The conditional gate must go to zero outside the elite range.

At y approximately 65%, create a single restrained mathematical transformation with a left baseline, a visible subtracted quantity and a right output: "S₀ = R_J + R_F" followed by "S = S₀ − λ_t g d". The subtraction is the key operation. The correction is not a positive risk penalty, nor an unconditional bonus applied to every contestant. Use a small teal underbrace beneath "λ_t g d" labeled "targeted reduction". Place "λ_t = βn" in small but readable type underneath, connected only to that correction term. Keep the correction term slightly darker than the surrounding formula.

To make the before/after visible without inventing a numerical result, draw two short aligned symbolic risk segments beneath the expression. Label them "baseline" and "protected case". The protected-case endpoint is shifted left by a short segment marked "λ_t g d"; there are no ticks or numeric lengths. A separate thin gray bypass from the AND junction goes directly to a third short label "otherwise S = S₀". Do not require three large panels for these states. The bypass is a mathematical alternative, not a failure path deserving a red cross.

At the rightmost edge of this mechanism, add the compact operation "Eliminate argmax S". An arrow from the corrected score formula ends at this operation; it must not emerge from the lower-risk illustrative segment as if the protected contestant is automatically eliminated. Make the text clear that larger S means worse combined standing. Beneath it, a very small note "deterministic ties" is optional if space permits. Do not change argmax to argmin and do not label the risk score a probability. The rule compares all current contestants; X merely demonstrates how one eligible contestant's score changes.

Temporal evaluation is a thin line at x=38–93%, y approximately 89%. Divide it once into an earlier segment and a later segment. Labels are "Tune: seasons 1–17" and "Test: seasons 18–34". Use a small vertical break and an arrow pointing from earlier to later time. A dashed connector from the MARS mechanism descends to this line, identifying evaluation of the designed rule. Do not feed test seasons back into tuning. Do not append success rates or imply that inferred elimination consistency is out-of-sample prediction.

Place one small note near the bottom-left margin: "Schematic · no empirical results shown". The figure contains illustrative mathematical geometry and symbolic contestant identities; it does not reproduce the paper's fitted values or prove that the revised rule is universally fairer. Do not include a statement such as more accurate, validated superior, unbiased or optimal fairness. The purpose is to expose the specific logic of reconstruction and targeted rule design, not to advertise results.

Detailed routing priorities: the raw-data arrow into the simplex should be short and vertical; the raw-data route into Plackett–Luce hugs the left boundary without touching the simplex vertices; the combined inference spine forks near x=32%; the upper branch approaches the matched-rule sheet from its left edge; the lower branch approaches only the fan outcome dot. The judge-outcome input may use a light gray elbow above the sheet and descend into its own dot, but it must not cut across the focal ranking rails. Design-evidence dashed lines approach the focal mechanism from its lower-left whitespace, not through the equations.

Palette: teal #327F87 means inferred support or the targeted protection operation; warm muted amber #B88745 marks the illustrative disagreement only; charcoal #293137 means observed records, ranks and text; gray #ADB2B3 means comparison structure or a bypass. Keep all broad spaces white. Do not alternate five unrelated colors across the five named activities. A lightly tinted feasible polygon and a narrow elite interval are enough fill for this entire image. The amber highlight must follow X's rank discrepancy, not become a new background panel.

Final content check before rendering: the inferred fan share is relative, complementary estimators share observations, two analyses branch in parallel, counterfactual comparisons hold the week and active set fixed, trait associations are not causal, the correction requires both conditions, g uses the positive part, d cannot be negative, λ_t depends on n, lower risk is a reduction and elimination uses argmax. Earlier and later seasons remain separated. No numerical measurement, vote total, actual participant result or performance claim has been invented. Preserve this scientific specificity while making the page visually quiet.

FINAL REDRAW CORRECTIONS, overriding conflicting routing details above. The supplied image is the second redesigned version. Preserve its open white composition, correct score equations, matched-week comparison and large selective rank mechanism. Only repair the following arrows and local annotations; do not restore any panels.

1. Remove EVERY long gray connector from the original weekly record. Remove the gray vertical line that currently passes through the Rule-conditional fan shares junction. The fan junction must receive exactly TWO arrowheads: feasible-set output from its left, and Plackett–Luce output from below. It must emit exactly TWO arrows: one to the weekly comparison and one to Inferred fan outcome. It has NO gray input and NO route to Judge outcome.

2. Convey the reused observed-judge input through two compact local source labels instead of crossing global routes. Above the weekly comparison place "observed judges" with a very short downward gray arrow directly to that comparison sheet. Immediately left of the Judge outcome dot place the short words "observed judges" with a short rightward gray arrow into the dot. Move the outcome pair about one label-height downward if necessary. These labels explicitly identify the original measurement and do not need another long connecting line. Keep Judge outcome and Inferred fan outcome aligned as two separate responses. The teal fan branch ends at the lower response only.

3. Likewise replace the long broken left-edge Plackett–Luce input route with a compact local source label "weekly observations" and a short gray arrow directly into the three candidate ticks. Remove the teal line that currently looks like it begins at the B vertex of the simplex. Plackett–Luce is NOT fed by a simplex vertex. Keep its point-estimate output connected by a clear teal line upward into the fan-share junction. Let the feasible-set triangle retain its own original-data input from above.

4. Delete the two risk-length mini charts if any return. Keep the baseline and corrected formulas as the visible before/after. Remove any full-region color rectangle behind the mechanism. Use the same white ground throughout. Keep the matched X marker safely inside the elite interval.

5. Make the design-evidence routes visible but subordinate: the rule-comparison output and the mixed-effects output travel independently into a small "design evidence" annotation beside the MARS area. They do not feed each other. Keep the actual score definition independent of any automatic fitted-parameter interpretation.

6. Remove the currently floating vertical dashed arrow above the later test segment. Write "Evaluate designed rule" over the center of the ENTIRE temporal line, with no long connector if an unambiguous one cannot fit. The Tune 1–17 and Test 18–34 labels and forward time line already express the evaluation. It is better to omit a redundant dangling arrow than to falsely target only the later test group.

Everything else remains source-grounded. This correction prioritizes exact endpoint semantics and legibility, not more objects or text. No new empirical claims.
