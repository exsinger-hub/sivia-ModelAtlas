Create one original publication-ready scientific overview illustration, not a poster, slide, dashboard, product interface, or collection of method cards. This is a manuscript-to-figure task. The manuscript-specific scientific specification below is the authority. Generate a completely new composition; nothing about an earlier generated layout has to survive. Output a single high-resolution landscape raster image on pure white, approximately 3:2. The intended placement is the end of the Introduction at 180 mm print width. The language of every visible label is English. Do not show a title banner, team number, contest award, institutional logo, model branding, filename, source-page thumbnail, or watermark. Use the short panel labels specified below, with the dominant scientific operation taking most of the area.

ART DIRECTION AND MANUSCRIPT TRANSLATION
Aim for the restrained precision of a carefully drawn journal methods figure: charcoal strokes, warm muted accent, a cool muted accent, and large clean white spaces that separate different operations. Make each line mean something. A background field, a border, an axis, a bracket and an arrow are different marks and must not be substituted for one another. The visual should still communicate its essential operation if the panel titles are hidden. Avoid shiny illustration, pseudo-three-dimensional props, gradient cards, drop shadows, decorative ribbons, stock science icons, anonymous neuron clouds, gears, trophies, shields, and thick arrows between isolated boxes. The actual entities and their change are the diagram. Only a genuine coordinate frame, table, matrix, state tuple or constraint boundary may create an enclosure. Do not place an enclosing rounded rectangle around every method. Do not distribute items evenly just because there are several methods. Reserve the densest and largest drawing for the paper's central relation, keep auxiliary methods quiet, and omit full derivations from visible text.

Think in an invisible 1500 by 1000 coordinate canvas with outer margins of approximately 60 units. Coordinates in this specification describe visual hierarchy, not text that should be drawn. Use comfortable margins within plots and between labels and edges. The shortest route that preserves meaning is preferred. A routed connector must end at a particular variable, operation or object, not an arbitrary panel boundary. Use small restrained arrowheads; no chevrons or arrow ribbons. If a connection would cross a label, move that connection into a reserved empty lane, preserving both its source and target. Do not delete a scientific feedback to avoid an overlap. No connector should run through an equation, rank list, organism, map cell label or boundary condition. Local labels must be adjacent to their own objects. Use a readable editorial sans serif for words and a compatible mathematical serif for variables. Sentence case, medium weight, dark text; no all-caps paragraphs or fashionable wide tracking.

TEXT AND SCALE DISCIPLINE
The manuscript-specific visible-label inventory below is intentionally much shorter than these drawing instructions. Print only the specified labels, equations, panel letters and tiny contextual notes, not the explanatory prose or source audit. Long source explanations are for your understanding, not for placement in the image. Most labels should be one to five words. Typeset mathematical subscripts distinctly, signs correctly, and operators with adequate spacing. The final drawing must not contain escaped LaTeX delimiters, raw backslashes, missing-glyph squares, duplicate words, gibberish, or a mix of near-identical variable names. At the intended print width aim for primary labels near 10 pt, ordinary labels near 9 pt, and exceptional notes at least 8 pt; shorten or reposition before making type smaller. Keep at most two text weights plus italic mathematics. Do not manufacture apparent detail by adding dozens of captions. Use the same symbol for the same entity each time, with an explicit time index where it changes.

EVIDENCE AND VISUAL INFERENCE
This is a conceptual reconstruction of a specific paper, not a new empirical analysis. Illustrative locations, curves and arrangements must be visibly schematic and must not look like measured results, fitted confidence bands or newly run simulations. Numerical quantities are permitted only when explicitly authorized below as mathematical endpoints, model constants or a labeled synthetic example. Do not create convincing-looking data, effect sizes, significance stars, forecasts, benchmark victories or performance percentages. A supplied formula defines a relationship; a source-reported claim is not permission to draw an invented dataset. Preserve boundaries between observed information, inferred variables, proposed controls, model assumptions and evaluation. Inconsistencies in the original paper must be handled exactly as specified, rather than silently inventing a corrected model.

QUALITY CRITERIA
Check every visible mathematical sign and direction against the manuscript-specific specification before completing the image. Check that an input does not become an output by accident, an association does not become a causal arrow, an estimate does not become an observation, and an illustrative example does not become a factual result. Check the whole composition in a reduced view: the dominant operation must remain identifiable, secondary branches must remain subordinate, and no dense footnote block may overwhelm the figure. The result should feel designed by someone who understood the scientific object, with deliberate asymmetry and generous negative space, not by a generator arranging a list of topics. The following detailed paper-specific instructions resolve any ambiguity in these general instructions.

MANUSCRIPT: BEHIND THE BALLROOM SCORES — INFER HIDDEN SUPPORT, AUDIT RULES, APPLY A SELECTIVE CORRECTION

Core figure claim. The paper infers relative fan support from observed judge scores and eliminations, compares rules on the same weekly contestants, and proposes a rank-sum correction that activates only when a judge-elite contestant is ranked worse by fans. The most distinctive operation is that conditional correction. Build a figure where a small inferred-share object feeds a fixed-week rule audit and then a large, transparent gate showing precisely who is changed and who is left alone. Do not make this a show-business graphic with dancers, stage lights, trophies, voting phones, ballot boxes or a justice scale.

Source grounding. Physical pages 4–6 define cleaning and the unobserved relative fan-share problem. Pages 7–10 specify a feasible-set interval model and a complementary Plackett–Luce point estimate, rule-conditional inference and internal checks. Pages 11–14 compare Rank, Percentage and Judges' Save within fixed historical active sets. Pages 14–18 compare judge and inferred-fan outcomes using mixed effects. Pages 18–20 define the MARS-Soft+Disagreement rule and early-season tuning / later-season evaluation. Pages 21–23 give sensitivity and limitations. Physical page 26 contains the full coefficient table. The artwork must not be a new season simulation, a true-vote reconstruction claim or an empirical performance chart. No numerical rescue-rate, accuracy or fairness claims are included.

The source has an internal sign inconsistency: Fig.4 prints argmin for the rank-sum rule although the text defines rank 1 as best. Section 7.2 unambiguously defines rank-sum elimination risk as larger=worse and eliminates argmax. Follow that explicit convention throughout. The artwork must never repeat the contradictory argmin on rank risk. The source's Percentage score is higher=better, so its elimination selects the smallest combined share. Keep those two conventions distinct instead of forcing all scores to the same visual direction.

Overall composition. Make a left-to-right but strongly unequal analytical layout on white. Panel a on x70–430,y160–600 is a compact latent-share inference drawing. Panel b on x500–1430,y160–830 is a wide fixed-week rule comparison and selective correction, taking roughly two thirds of the page. Within panel b, a small rule-audit strip sits above a larger gate-and-score-change drawing. It is one coherent weekly decision object, not separate cards for every method. A quiet lower-left support area x70–470,y730–870 acknowledges the two-outcome mixed-effects analysis without stealing focus. Use panel labels "a  Infer relative fan support" and "b  Compare rules on the same week". The largest local label inside b is "Selective rank correction". No giant acronym title.

Persistent identity is crucial. If symbolic contestants A, B and C appear in the inference or weekly score object, they must remain the same identifiers in later parts. Do not use a colorful mascot or stock person icon for each contestant. A letter and a thin stroke are enough. Values are symbolic unless an explicitly labelled synthetic example is requested. In this design no numeric contestant score is authorized; any arrows show the direction of a mathematical correction, not an observed elimination. The statement "Schematic week; no contestant results shown" appears below the main object.

INFERENCE OBJECT
At the left, place two observed inputs as two short stacked labels: "Judges' totals  J_i" and "Observed exits  E". A thin brace groups them as "One historical week". Below, a small note "Fan totals hidden" explains what is not observed. Do not show a fan-vote column with actual numbers or a pie chart pretending to expose the votes. A small open triangular simplex represents possible fan-share allocations for a three-contestant illustration. The corners are labelled p_A, p_B and p_C. The triangle is a visualization of p_i≥0 and Σ_i p_i=1, not a geometry specific to every week; actual contestant count varies. Put the short label "Illustrative simplex" underneath.

Inside the triangle, mark a lightly shaded feasible polygon cut by one or two elimination constraints. Its exact geometric coordinates are not empirical. Use thin diagonal boundary lines and a pale muted-blue fill. The geometry should look like a subset of a simplex rather than a cloud of points. Show one small open circle inside the polygon labelled "Max-entropy representative". It is one selected feasible allocation, not the unique true fan vote. Do not put the representative outside the feasible region. Do not add axes, grid labels or numeric probability percentages to the triangle.

A concise expression beside the triangle gives the shared score:
"C_i = w j_i + (1 − w)p_i"
and a second short line gives the elimination constraint:
"C_e ≤ C_survivor"
Here j_i is the normalized judge signal and p_i is the latent fan share. Higher C is better; an observed eliminated contestant has no greater combined score than survivors in the feasibility model. Do not reverse this inequality to match the later rank-risk convention. Label the shaded region "Feasible shares". Under it, depict three short horizontal interval marks labelled A, B, C with endpoints L_i and U_i only on one representative row, not numeric intervals. These are feasible bounds, not calibrated Bayesian credible intervals or sampling confidence intervals.

A parallel, very small branch from the same observed inputs leads to the words "Plackett–Luce point estimate". It may be drawn as one point p̂ rather than a second full triangle. The two inference methods are complementary; they do not form a serial A→B pipeline. The maximum-entropy representative belongs to the feasible model; the Plackett–Luce point estimate belongs to the likelihood model. Do not label the likelihood point as max entropy or merge all intervals into a posterior distribution. Avoid filling the triangle with three differently colored model badges.

The point model's source likelihood is based on exp(−κC) for elimination ordering. That negative sign makes lower combined scores more likely to be eliminated. The full likelihood product is too long for the figure and should remain off-canvas. If any likelihood expression is needed, use the short proportionality "P(exit i) ∝ exp(−κC_i)" only for a single elimination from a fixed remaining set; do not imply it covers the full ordered multi-exit likelihood without normalization. Prefer the method label and point object to keep typography calm.

At the output of the two branches show a short paired label "Rule-conditional shares  F_R / F_P". These are estimates inferred under Rank and Percentage assumptions. They feed the same-week audit. Do not turn them into absolute vote totals or two separate sets of real people. A tiny note by the output says "Inference uncertainty retained". This is about the feasible-share set and rule dependence; it does not claim uncertainty was fully propagated through every later analysis.

The source's internal elimination-consistency checks reuse the eliminations that constrained inference, and therefore are not prediction accuracy. No 97% or 99% accuracy badge should appear. The source also performs a limited one-week-ahead sanity check, but this schematic deliberately does not reproduce its results. Do not add a train/test arrow to the entire inference drawing implying fully prospective validation. The later season split applies specifically to the proposed-rule tuning/evaluation.

FIXED-WEEK COMPARISON, UPPER PART OF PANEL B
Draw one short horizontal rail labelled "Same active contestants · same judges' scores". Alongside it print "Cross-apply F_R and F_P". That rail is the shared input to the three compared rules. Do not change the contestant pool between rule rows. The counterfactual is local to an observed week; it is not a replay of a whole season with future scores imputed. No tournament bracket, season champion or changing active-set tree should be drawn.

Below the rail, show three concise open rows, each with its rule name, mathematical operation and elimination direction. Use thin row separators, not a colored card per rule:
"Rank: R_J + R_F → largest risk"
"Percentage: judge share + fan share → smallest share"
"Judges' Save: bottom two by Percentage → lower judges' total"
The rank row uses ranks with 1=best. Percentage is a score where larger is better. Judges' Save is the paper's deterministic implementation, not the actual show's discretionary choice. Keep the words "bottom two" attached to Percentage rather than the Rank row. Do not make Judges' Save a fourth data input or a universal correction that protects every judge-favored contestant.

The shared-input rail fans into these rows with short local connectors. Their outputs terminate at a small bracket labelled "Agreement / conflict". This is an audit, not a ranked list of winning algorithms. No sample eliminated contestant letter needs to be shown; without numeric scores such an outcome would be invented. The point of this upper strip is that the same information is recombined three ways. Keep it compact and legible so the selective correction below remains the dominant operation.

The source reports “variance engulfing”: close judges' totals plus uneven fan support can change elimination under Percentage. This can be acknowledged by the tiny phrase "Magnitude versus rank" near the comparison bracket, without a simulated density plot. Do not draw a fitted fan-spread heatmap or reproduce source percentages from memory. A paired short-bar pictogram would be acceptable only if explicitly symbolic, but it is lower priority than the fixed-input identity and opposite elimination directions. Prefer omitting it to adding visual noise.

SELECTIVE RANK CORRECTION — THE CENTRAL MECHANISM
Use a large open rank-score transformation underneath the rule strip. On its left, a compact paired-rank object has two columns headed "Judge rank  R_J" and "Fan rank  R_F". The rank orientation is labelled "1 = best". Instead of numerical rows, show two symbolic cases using repeated contestant markers:
"Judge-elite, fan-disfavored"
"Other contestants"
These are mutually defined logical cases, not outcome categories assigned from actual data. A tiny bracket marks the top-α region of the judge rank axis. A line from the fan column to the first case indicates fan rank worse than judge rank. Do not draw a giant logic diamond surrounded by boxes. The gate can be a clean brace with the two conditions stacked and a small “AND” between them.

The exact activation conditions are:
"q_i < α"
"R_F(i) > R_J(i)"
where q_i=(R_J(i)−1)/(n−1). Print q_i definition immediately below the top-α bracket. A thin line at q_i=α defines the boundary. At the threshold q_i=α the soft factor is zero; the active region is strictly below. If the prose says “judge elite” without a strict symbol, the formula must still use the positive part correctly. Do not use an OR gate. A high judge rank alone is insufficient, and fan disadvantage alone is insufficient.

From the combined gate, use one muted-rust path to the soft adjustment and one neutral bypass around it. The bypass represents any case where one or both conditions fail. Label it "Otherwise: no change". It should run along a short quiet route, not disappear. This unchanged branch is as important as the active branch because the proposed mechanism is selective. The active path label is "Protection > 0"; it decreases elimination risk, not fan votes or the judge score. Do not show a stamp saying “saved” or guarantee that a protected contestant survives; the adjusted score is still compared with everyone else's.

At the center of this mechanism print three short definitions in a aligned compact set:
"g_i = (1 − q_i/α)_+^p"
"d_i = max(0, R_F(i) − R_J(i))/(n − 1)"
"λ_t = β n"
The first factor selects and smoothly weights judge elite. The second is the normalized fan-disagreement magnitude. The scale λ_t adjusts to active contestant count. Use a superscript p only on the positive-part term; do not turn p into fan share or a probability. A tiny legend says "(x)_+ = max(x,0)" if needed. These are algebraic definitions, not variables to put in three individual colored modules.

At the right, the main large expression is
"S_i = S_i⁰ − λ_t g_i d_i"
with the baseline directly above:
"S_i⁰ = R_J(i) + R_F(i)"
The minus sign and multiplicative g_i d_i must be exact. Show a simple horizontal risk axis with an open baseline marker S_i⁰ and a filled adjusted marker S_i to its left. A short left-pointing rust arrow between them represents the reduction in risk when the gate is active. Label the axis "Elimination risk →". The adjustment goes left, against increasing risk; this is visually more informative than a shield icon. No numbered tick marks or invented score values appear. Label this little graphic "Active gate: risk reduced", not “rescued contestant”.

Below, draw a second shorter symbolic risk line for the bypass with two coincident open/filled markers and label "Inactive gate: S_i = S_i⁰". This is a meaningful before/after contrast, not another card. The two lines share the same orientation but need not imply equal original risk values. Their marks are symbolic. Do not show the inactive marker moving or the active marker moving right. Use the same S notation consistently, including the superscript zero for the baseline and subscript i for the contestant.

After these lines, a small final bracket reads "Eliminate argmax_i S_i". This is the endpoint of the rule. All candidates, adjusted or unchanged, are ranked together. The inactive branch joins the same final comparison. Do not send only the active branch to argmax and discard the rest of the contestants. A short additional phrase "Deterministic ties" is sufficient; the full tie-breaking list does not belong in the figure. If the exact formula is hard to typeset, “Eliminate largest S_i” is preferred to a malformed mathematical operator.

EVALUATION BOUNDARY AND SUPPORTING ANALYSIS
Below the gate mechanism, place a small two-part chronological rule-tuning strip: "Tune: seasons 1–17" then a vertical separator then "Evaluate: seasons 18–34". Use a thin neutral line, not a large timeline with 34 ticks. This split applies to selecting the proposed rule's α,β,p settings, not to every modelling step in the paper. Do not print result percentages, a performance chart or a green winning badge. A small label "Week-level counterfactuals" under the strip is mandatory. The source cannot reconstruct complete season trajectories without future score imputation; the figure must not imply otherwise.

In the lower-left support area, show the same pair-week observation splitting into two labelled outcomes, "Observed judges" and "Inferred fan support". Use a short two-row shared-effects notation "traits + season + pro + celebrity". Under it, the label "Mixed-effects comparison" explains the purpose. Traits and season are fixed effects; pro and celebrity are random intercepts. If there is space, distinguish them using a brace with "fixed" under traits/season and "random intercepts" under pro/celebrity. Do not create a generic neural network, causal DAG or an independent feature-importance bar chart.

A fine connector from the inferred-share output in panel a can reach "Inferred fan support" in this support area. The judges outcome comes from observed totals, not from the fan estimate. The source models the inferred fan point estimate without measurement error, so append the concise note "Point estimates; uncertainty not propagated". Do not show confidence envelopes suggesting a joint uncertainty-aware fit. The support area terminates in a comparison of outcome associations, not in a causal arrow that changes the MARS gate. The proposed rule is not directly defined from fitted pro/celebrity effects.

Palette. Use muted blue for latent-share feasibility, charcoal for observed data and rule expressions, and muted rust only for the active protection correction. Keep unchanged paths gray. No glitter, stage colors or a separate pastel background for every method. Thin constraints and rank boundaries can be dark blue; the risk-reduction arrow is the strongest accent. The top rule-comparison strip stays mostly black and gray. Thus the eye first sees the selective change and then traces its inputs back to the inferred share object.

Routing. Inference branches share one source and do not connect serially. The inferred outputs connect to the fixed-week rail. The three ordinary rules are recombinations of that rail, not three independent datasets. Their audit supports the motivation for the selective correction, but the new rule itself takes judge/fan ranks. The gate has an active path and a bypass; both rejoin at final risk ordering. The mixed-effects branch is lower priority and never blocks the central left-to-right reading. No connector may cross the simplex, a rank formula or the risk axis.

Priorities for space. Retain the feasible-share uncertainty, shared weekly inputs, AND gate, baseline-minus-protection score, unchanged branch, argmax direction and local evaluation boundary. Shorten algorithm explanations before shrinking those labels. It is acceptable to omit the full likelihood, full mixed-effects equations, adjustment hyperparameter values and ancillary diagnostics. The figure is an overview of the model relationships, not a compressed equation appendix. More formulas are not automatically more detail; the gate and risk change are the indispensable detail.

Source-language checks. Relative fan support is not true votes. Feasible intervals are not automatically probabilistic credible intervals. F_R/F_P refer to inference rules, not groups of fans. Rank 1 is best, rank-sum risk is larger=worse, and Percentage combined share is smaller=worse. Protection does not alter rank observations; it modifies the new rule's risk score. A judge-favored contestant who is not fan-disfavored receives no bonus. A fan-disfavored contestant outside the judge-elite region also receives no bonus. The competition still eliminates the largest adjusted risk. These cases must be obvious from the image even if all headings are covered.

The final work should look like a carefully reasoned inference-and-decision figure in a statistical modelling paper: few motifs, precise identities, a visible conditional operation, and no generic module scenery. Keep small text out of the lower corners, maintain clean mathematical typography, and resist filling remaining white space with decorative plotting. The source and accompanying audit are available for details; the image should expose the scientific logic rather than reproduce every section.
