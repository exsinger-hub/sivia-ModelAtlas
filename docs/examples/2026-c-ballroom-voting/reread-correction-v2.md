Make a precise local correction to the attached voting-rule figure. Preserve the scientific arrangement and source-grounded equations, but remove the repeated card treatment and fix these specific defects.
1. Erase ALL rounded-rectangle backgrounds and borders: behind the shared-input rail, rule-conditional shares, judge-rank/fan-rank area, judge-elite label, other-contestants label, active-condition formula area, active/inactive risk areas, and mixed-effects support. Keep genuine table separator rules, simplex edges, rank columns, braces, axes and connectors. Use pure white behind all mathematics. Panel separation should come from space, not pastel blocks or dashed enclosures.
2. Correct the soft-gate formula to exactly g_i=(1−q_i/α)_+^p. There are NO prime marks on q_i or α. Preserve q_i=(R_J(i)−1)/(n−1), d_i=max(0,R_F(i)−R_J(i))/(n−1), λ_t=βn, AND conditions q_i<α and R_F(i)>R_J(i).
3. In the simplex, shade only a single clearly bounded feasible polygon containing the max-entropy representative, not the entire triangle. Keep the rest white. Label the shaded polygon Feasible shares. The representative circle must lie inside. Preserve p_A,p_B,p_C and the interval bounds.
4. Add one thin named connector from the Rule-conditional shares F_R/F_P output to the top shared-week comparison rail; route it in the empty vertical gap between panels. Retain the branch into the selective correction. The same inferred shares are inputs to both, not only the gate.
5. Keep both risk axes: larger risk goes right, active correction shifts S_i left of S_i^0, inactive markers coincide, both branches join Eliminate argmax_i S_i. Never change largest-risk elimination to argmin.
6. The lower-left support is a two-outcome mixed-effects fit. Replace its two arrows currently pointing from outcomes into effects with one shared-effects bracket on the left feeding the two OUTCOME labels on the right: traits+season [fixed] and pro+celebrity [random intercepts] → Observed judges / Inferred fan support. Keep "Point estimates; uncertainty not propagated". This is statistical model specification, not a causal claim.
Use muted rust only for the active correction and dark blue for inferred-share geometry; all support text charcoal. The result should be an open mathematical plate with generous whitespace, not panels of dashboard components. Do not add examples or numerical results.
