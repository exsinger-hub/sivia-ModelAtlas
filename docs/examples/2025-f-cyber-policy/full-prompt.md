# Complete ImageGen prompt record

## Call 1 · generate

Output: `overview-v1.png` · artifact `exec-9c88d354-b53e-48bb-95e6-963a51dc9935.png`.

Use case: infographic-diagram
Asset: a paper-specific scientific Overview, English, landscape 1536 x 1024, intended full-text-width 180 mm at the end of Introduction.
Purpose and authority: Explain the KDMF framework of the paper "Cracking the Cyber-Puzzle: KDMF in Action" (2025 ICM F). Scientific authority is the full paper, not a generic cybersecurity workflow. Three complementary evidence streams converge on a policy brief: country profiling, policy evaluation, and contextual associations. The design must show parallel branches, not pretend K-means feeds LDA or MIC.

Composition: white editorial canvas, generous margins, refined dark navy typography, teal for descriptive profiles, muted violet for policy evaluation, warm amber for demographic associations. Top 16% shared evidence inputs. Middle 63% three equally readable, distinctive method scenes. Bottom 21% a restrained convergence into a short policy brief plus robustness checks. Minimal rounded containers; illustrative meaningful data/document objects dominate, not paragraph cards. Title: "Evidence for cybersecurity policy".

Top evidence band, exactly three groups: "Country indicators" with four small readable labels "NCSI", "CEI", "Legal measures", "Reporting"; "Policy texts" with a pair of legible abstract legal document sheets; "Demographics" with simple people, education and network icons. No real flags or maps indicating rankings.

Left teal stream, title "Describe country profiles". A small input chip explicitly says "NCSI + legal measures". This is the ONLY input into "K-means"; do not show all four indicators entering clustering. A simple unlabeled point-cloud schematic, three color groups without axes, numbers, fitted statistics or country ranks, labeled "Schematic clusters". Underneath, small labels "Elbow + silhouette" and "PCA view" connect to "Country profiles". Country profiles are descriptive, not a causal explanation.

Center violet stream, title "Evaluate policy changes". Policy-text documents -> "LDA: six themes". Display six short themes as two tidy rows of three tags: "Criminal law", "Data privacy", "Operator duties", "Infrastructure", "Emergency response", "Cross-border data". Then a clearly conceptual 2 x 2 comparison matrix with headers "Before", "After", rows "Treated", "Control"; use clean cells with no plotted values. Label it "Difference-in-differences". Arrow to "Estimated policy associations". Do not assert proven effectiveness, reduction percentages, statistical significance or verified parallel trends.

Right amber stream, title "Relate national context". People, network, graduation cap and economic document glyphs -> "MIC" and "Factor analysis + regression" as complementary methods inside the same stream, with one grouping bracket -> "Context associations". Short input labels "Economy", "Connectivity", "Education". No causal arrows between individual social factors, no best/worst country claims.

Connections: input band country indicators feeds descriptive stream and is also shared evidence for policy evaluation, using a thin labeled shared-data bracket if necessary. Policy texts feed LDA; LDA categorizes policy changes for DID; demographics feed the context branch and provide "Covariates" to DID via one clearly labeled thin lateral arrow with a dedicated gutter. Avoid crossings. Three stream outputs each have one short downward arrow to a wide bottom strip title "Policy brief". Three concise output phrases: "Profile differences", "Policy estimates", "Context-aware priorities". A small separate footer beneath reads "Robustness: coefficient perturbation · input-noise tests". A small note at bottom right: "Method overview · no numerical results". No arrow implying association is causation.

Typography: consistent modern sans serif, bold short section titles, readable labels, no mini paragraphs. Prefer whitespace and shorten phrasing instead of microscopic type. All scientific labels must be spelled correctly, especially "Difference-in-differences" and "K-means". No numbered decorative badges.

Evidence policy: source sections4(pp6–12),5(pp13–17),6(pp17–20),7(p21). Methods only. The source contains internally inconsistent interpretations of some policy-effect signs; do NOT visualize those effects, numerical coefficients, efficacy rankings or claim causal validation. The matrix and clusters are explicitly schematic.

Reference adaptation: borrow the inspected source overview's division into three tasks and joint advice, not its looping decorative arrows, source pixels, long captions, country ranking maps or typography. No team IDs, author names, contest/award badges, institutional logos, watermarks or copied charts.

Final checks: country profile input specifically NCSI + legal measures; all three branches remain complementary; six readable policy themes; DID treated/control before/after; MIC not causal; all branches converge on policy brief; no fake numbers or ambiguous arrows.

## Call 2 · edit

Input: `overview-v1.png`.

Output: `overview.png` · artifact `exec-477b9ce6-523e-443a-abc8-8a478b43bce6.png`.

Use case: precise-object-edit. Input is the draft cybersecurity methods overview. Preserve all three columns, semantic colors, exact methodological labels, illustration content, theme names, and layout.
Correct the background and one dependency:
- Replace ALL smoky, blurry, shaded dark background outside the panels with solid pure white (#FFFFFF). No gray vignette, no dark halo, no gradient, no drop shadows. Panel interiors remain very pale teal/lavender/cream. Main title becomes crisp solid navy on white. Footer note becomes readable navy. Headers and body keep high contrast.
- The amber Covariates arrow currently points into "Estimated policy associations", which is wrong. Keep its route in the right gutter, shorten its vertical leg and point its arrowhead into the RIGHT BORDER of the "Difference-in-differences" matrix/method box instead. It must end at the DID analysis, not the policy-associations result. Keep demographics as its origin.
- Make the shared-data line unambiguous: country indicators contribute to the DID analysis, not to the demographics stream. Stop the shared indicator line at the central policy branch; do not merge it with the amber covariate line.
Invariants: NCSI + legal measures are the only clustering input; K-means descriptive clusters; six LDA themes; before/after treated/control DID; MIC and factor analysis + regression are context associations; three branches merge into Policy brief. No empirical metrics, causal proof, team IDs, badges, or new scientific content. White clean journal page, not a dark cinematic poster.

## Round 3 — mechanism-focused redesign

Exact submitted text. ImageGen edit; input: `overview-v2.png`; output: `overview-v3.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Borrow visual principles from the inspected Sivia examples without copying their layouts or subject matter. Useful principles are persistent entity identities, a visible before/after state, a dominant mechanism with asymmetric supporting material, and feedback attached to the state that is actually updated. Do not import the examples' language-model internals, reinforcement learning, benchmark claims, floating metrics or decorative dashboards. Some Sivia examples themselves contain many panels; that is not the principle being requested. The adaptation here is the density of scientific explanation inside a well-composed scene, not the number of rounded rectangles.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: CRACKING THE CYBER-PUZZLE — a source-grounded ICM 2025 F overview.

Scientific purpose: show how a shared country-level evidence base supports three distinct analyses: national cybersecurity profiles, policy-change comparisons and socioeconomic associations. Policy texts describe what changes; before/after and treated/control outcomes define the evaluation contrast; other factors characterize associations rather than automatically establish causes. This is based on Team 2513705, Cracking the Cyber-Puzzle: KDMF in Action, an Outstanding Winner paper with 33 physical PDF pages. Sections 4–6 cover the analyses on pages 6–20; section 7 checks sensitivity on page 21; limitations and memo appear on pages 22–25. The source has inconsistent interpretations of some policy coefficient signs, so the figure must not display an empirical effect direction or claim a positive causal result. Do not print provenance details.

Aesthetic direction: replace the three equal framed columns and generic policy cards with an open evidence diagram built around ONE country-by-year ledger. A small projection of national profiles branches left; a policy-text strand approaches from above; the largest object to the right is a four-cell before/after contrast with symbolic outcomes. A narrower social-factor relation remains below the ledger. These objects should differ in geometry because they perform different operations. A real ledger, a text-to-topic mapping and a difference-of-differences contrast are more informative than shields, offices, folders and random country icons.

Canvas: landscape about 3:2, flat white, comfortable five-percent margins. Put the restrained heading "Cybersecurity policy: assembling the evidence" at x=5%, y=5%. The central evidence ledger occupies x=25–50%, y=33–62%. National profiles use the smaller left area x=5–23%, y=35–69%. The focal policy contrast occupies x=56–95%, y=36–73%. The policy-text strand is a shallow, irregular strip above the ledger and contrast, approximately y=17–29%. A compact socioeconomic analysis sits below the ledger at y=75–87%, and a small synthesis statement sits near the lower-right. Do not enclose these coordinates in colored rectangles.

Identity and color: teal #327F87 identifies the treated group in the comparison, while warm amber #B88745 identifies the control group. These are group-role colors, not judgments of good or bad policy. Neutral charcoal and gray carry country profiles, document traces and covariates. In the clustering projection, use shape distinctions plus faint neutral hulls instead of reusing treated/control colors as cluster assignments. Treatment groups and K-means clusters are not the same thing; do not imply a causal connection between those groupings through shared color.

Evidence ledger: draw a compact table with a Country column, a Year column and a few symbolic measure columns. The Country entries are neutral A, B and C, each repeated across a pre and post row. These are explanatory placeholders, not named countries or actual observations. The Year entries are "pre" and "post", with a small brace above them labeled "country × year". Avoid invented dates and numerical scores. A small header over the measure side reads "Indicators / covariates". Within the cells use short gray dashes and small hollow observation marks rather than unreadable fake decimals.

The indicator labels "NCSI" and "Legal measures" must appear as the two specifically highlighted columns of the ledger. Give their headers a very light neutral underline, not a thick colored band. A small adjacent note lists "Exposure · reporting" as additional recorded indicators, with no arrow implying they are also used for K-means. The source introduces a wider indicator set but clusters using only standardized NCSI and Legal Measures. The figure must make that input selection visible through two short column leaders, not silently send the entire table into clustering.

Show tiny pair braces at the left edge of the repeated pre/post rows so the viewer can recognize the same country across time. One or two matching hollow symbols can recur in the later comparison. Do not populate the ledger with fabricated country data or imply that there are only three countries in the paper. A very small "symbolic records" note below the table can clarify that these rows expose the data structure. This note can be merged into the global schematic note if another local label is too much.

At the upper left of the ledger, a short annotation "aligned definitions / reporting" identifies the data-conditioning step. It is not a new preprocessing module. The underreporting limitation means recorded cybercrime does not equal true incident incidence. Do not draw a complete-data checkmark, a validated shield or a map claiming comprehensive global coverage. A discreet small open gap in one gray table cell can remind the viewer that completeness is not guaranteed, but avoid prominent error symbols that would misrepresent actual missingness counts.

National-profile branch: two thin gray leaders leave only the NCSI and Legal measures columns and converge on a short label "standardize", then "K-means · 3 groups". Draw a small 2-D schematic projection beside it, with axes labeled only "PC1" and "PC2". Arrange three sparse groups of tiny circle, triangle and square marks. Their locations are illustrative, not PCA results from the source data; write "schematic projection" directly below the axes. Do not use precise tick marks, explained-variance percentages, silhouette values or a numeric elbow plot.

The cluster projection should be smaller than the policy contrast and should not dominate the figure with colored bubbles. Three very faint open contours can distinguish the groups, but no opaque circular background enclosing all dots. Keep about four or five representative marks per group, not a dense synthetic scatterplot that looks empirical. Use shape coding deliberately: circle, triangle and square identify profile groups only in this branch. Never carry those group identities into the treated/control rows unless explicitly explained, because they are a different grouping operation.

Below the projection add "national profiles" and a short smaller note "elbow / silhouette checks". This names source diagnostics without claiming a score or that three groups are universally optimal. The output is a descriptive grouping, not a policy treatment assignment. Do not route the clustering output into the LDA text or into the treated/control cells. Its only downstream route is a thin dashed synthesis link to the lower-right conclusion area.

Policy text enters independently along the top: draw two small overlapping document traces, each consisting of five short horizontal lines, with a few highlighted phrase spans. These are stylized text carriers, not fake legal quotations. Next to them write "Policy texts". A light extraction leader moves from the highlighted spans to six concise topic labels set along two short rows, without placing each topic in a pill or card. The labels are "Criminal law", "Data privacy", "Operator duties", "Infrastructure", "Emergency response", and "Cross-border data".

Place "LDA · six themes" above these topic labels in small type. The six topics are a description of document content, not six quantified causal effects. Do not draw word clouds, network-degree scores, topic probabilities or a bar chart of topic weights without data. The phrase highlights use muted gray with a tiny teal edge if needed, but not the full treatment/control palette. The important visual transformation is text spans becoming a named policy theme, not six separate departments connected in a chain.

A thin brace under the six topics leads rightward to the label "policy change". This label ends at a small vertical time marker on the focal before/after contrast. The topic mapping provides policy context; it does not itself generate treatment assignment through a deterministic arrow from topic number to group. Put the short qualification "policy context" on the connecting leader. This stops the diagram from claiming that LDA estimates a causal policy effect.

Focal comparison at x=58–94%, y=37–71%: draw a clean two-by-two arrangement with only thin horizontal and vertical rules. Column headings are "Before" and "After". Row headings are "Treated" and "Control". The row identifier marks are teal for treated and amber for control, matched to small role markers at the corresponding input boundary of the ledger. The role markers are not the cluster shapes and do not refer to actual named countries. Preserve a little more horizontal space between Before and After than between the row labels and their cells.

In the four cells, write exactly the symbolic quantities "Y_T,pre", "Y_T,post", "Y_C,pre", and "Y_C,post", with T/C and pre/post as clear subscripts if possible. These are outcomes, not coefficients or estimated values. Do not print actual incident rates, a sign, a percent reduction or an upward or downward fitted curve. The symbolic contrast is the scientific detail; the viewer should be able to understand the comparison without a decorative trend plot.

Draw one slim horizontal arrow across the treated row from before to after. Above that arrow print "Δ_T". Draw the equivalent arrow across the control row and label it "Δ_C". Both arrows mean a time difference, not improvement, so neither has an upward or downward direction. Their colors follow their row roles. Keep the arrows well away from the symbolic subscripts so they cannot be mistaken for minus signs.

Beneath the four-cell structure, bring two short vertical leaders, one from the treated change and one from the control change, to the single expression "DID = Δ_T − Δ_C". The subtraction sign must be visually distinct and correct. Under it, in smaller text, print "difference of changes". If the cell expressions are legible, the expanded difference formula need not also appear. Avoid doubling the same algebra into a crowded lower panel. The focal contrast should contain no pastel background, thick border, glossy header or three-dimensional chart.

The country-year ledger supplies the outcomes to this focal comparison through one clear neutral connector labeled "matched outcome records". Its arrow ends at the left boundary of the two rows, not at the DID expression itself; this exposes the fact that both groups and both periods are inputs. Treatment/control comparability is a modeling assumption, not something established by the illustration. A small qualifier immediately below the DID expression reads "comparability / reporting limits". There is no green check claiming identification success.

The source uses covariates in the policy evaluation. Represent them as a small shared covariate brace below the ledger, labeled "GDP · connectivity · education". A thin leader from this brace enters the comparison from below, labeled "adjustment". It should not enter only the treated row, which would imply different measurements for controls. Make this connection thinner than the main matched-record path. Do not render regression coefficient estimates or a causal graph with unsupported conditional independencies.

A separate small annotation "legal capacity / cooperation" can join the adjustment brace if space permits. It is supporting context for the evaluation, not a fourth analytic pillar. Do not force every covariate into its own rectangle. A concise label beside a shared brace carries more meaning and leaves space for the focal difference operation. Keep the overall covariate label count within approximately two readable lines, not a paragraph of methodological caveats.

Socioeconomic association branch below the ledger: begin at the covariate brace, not at the DID output. Show a compact three-column symbolic array or strip: neutral attribute marks on the left, an association-strength relation labeled "MIC" in the center, and a smaller joint-model annotation on the right. The attribute labels may be grouped as "income / connectivity / education", reflecting the source's socioeconomic variables. The array must not contain invented coefficients or colored heatmap cells that resemble measurements.

After the MIC relation, draw a slim descriptive continuation labeled "factor analysis + regression". Do not imply that MIC itself proves causal influence or that all source variables were retained with known coefficient signs. The output label is "associated factors". Add "not causal effects" in smaller gray text directly beneath it. This branch can be represented through a narrow mathematical relation rather than another large table, so its visual footprint is different from the focal DID matrix.

Keep the covariate pathways scientifically separate: a shared covariate bundle supports the policy adjustment and also enters the association analysis, but the DID coefficient is not an input to MIC. The national clusters are not automatically the treated/control groups. LDA themes describe the policy documents and do not become numeric incident outcomes. These distinctions should be clear from route endpoints even if a reader skips the small limitation labels.

Synthesis at the lower-right should occupy no more than roughly a tenth of the inked page. Use three short incoming dashed leaders from national profiles, the policy comparison and associated factors. They end beside a small three-line annotation headed "Policy interpretation". The three lines are "profile context", "change comparison", and "associated factors". No boxes, memo-card stack, official seal or recommendation trophy. The labels explain how the evidence is brought together without inventing a ranked policy recommendation.

If the synthesis area needs a visual object, use a single short bracket collecting those three evidence lines, not a policy document with fake prose. The user is interested in the figure, not in a flowchart ending with a generic report. The focal DID operation should remain much larger and more explicit than this final synthesis annotation. Avoid the phrase proven effectiveness, optimal policy, causal discovery or validated impact.

Sensitivity details remain small margin annotations: "DID coefficient perturbation" connects to the comparison's output, and "MIC input-noise check" connects to the MIC relation. They are diagnostics of two different analysis routes; do not put them into one unlabeled loop that suggests retraining every model. No sensitivity curves, numeric error bars, p-values or invented robustness scores. Place these notes near the relevant object instead of a full-width bottom metric strip.

Reporting caveat: recorded indicators can differ from actual cybercrime burden because of underreporting and national measurement differences. This should be acknowledged with one concise "reporting limits" phrase next to the ledger/comparison, not a red warning banner or a defensive paragraph. The source's internal inconsistency in coefficient-sign interpretation is handled by omitting effect directions entirely. The illustration remains informative because it depicts how contrasts are constructed, not because it asserts a favorable result.

Line routing: policy texts and topic labels occupy the top band with short local leaders; their policy-context route lands on the time boundary between Before and After. The central ledger's two highlighted columns connect left to standardization and clustering, using one shared corner rather than crossing the main outcomes route. The outcomes route goes right into the comparison row boundary. Covariate adjustment approaches from below. The MIC branch sits underneath the ledger; its outgoing synthesis route travels through bottom whitespace. The left profile-synthesis connector can run along the lower outer margin, clearly dashed, with no implied input to MIC.

Space and grouping: the profile projection has its own axes but no enclosing card. The ledger has only the rules a table needs. The topic words align loosely with their text extraction braces but do not become a row of six pills. The comparison has a structural matrix, not a decorative frame. The association strip uses a brace and a small relational stroke. These different object silhouettes prevent the page from becoming six identical module boxes while retaining a clear reading path.

Typography: keep the main heading smaller than in the supplied old image. Use no all-caps banner names such as GLOBAL CYBERSECURITY FRAMEWORK. Local labels "National profiles", "Policy change comparison" and "Associated factors" can be medium weight and sit directly beside their objects. Use consistent subscript formatting for Y_T,pre, Y_T,post, Y_C,pre and Y_C,post, and clear Greek delta on both change arrows. If the model cannot render a subscript cleanly, a plain readable Y(T, pre) format is preferable to corrupted notation, but all four cells must use the same convention.

Reference adaptation: borrow Sivia's practice of carrying identifiable evidence through a transformation, and of allocating space to the actual mechanism rather than to every named method equally. Here the persistent evidence is a country observed across time, not a video frame or language-model token. The before/after relation is a symbolic policy contrast, not a benchmark performance chart. Do not copy a multimodal-agent funnel, neural reasoning workspace or reinforcement-learning architecture.

Final review should verify that exactly two source indicators feed K-means; three profile groups are not confused with treated/control; six LDA themes remain descriptive; the four outcomes are in their correct cells; Δ_T and Δ_C are within-group time changes; DID subtracts control change from treated change; covariate adjustment is shared; social associations do not feed the DID coefficient backward; and no numerical effect or sign is asserted. At thumbnail size the eye should see evidence rows, document spans and a large paired contrast, not a row of interchangeable colored modules. Place one small bottom-left note "Schematic · symbolic outcomes, not estimates". Return the finished illustration only.


## Round 4 — inspected correction

Exact submitted text. ImageGen edit; input: `overview-v3.png`; output: `overview-v4.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Borrow visual principles from the inspected Sivia examples without copying their layouts or subject matter. Useful principles are persistent entity identities, a visible before/after state, a dominant mechanism with asymmetric supporting material, and feedback attached to the state that is actually updated. Do not import the examples' language-model internals, reinforcement learning, benchmark claims, floating metrics or decorative dashboards. Some Sivia examples themselves contain many panels; that is not the principle being requested. The adaptation here is the density of scientific explanation inside a well-composed scene, not the number of rounded rectangles.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: CRACKING THE CYBER-PUZZLE — a source-grounded ICM 2025 F overview.

Scientific purpose: show how a shared country-level evidence base supports three distinct analyses: national cybersecurity profiles, policy-change comparisons and socioeconomic associations. Policy texts describe what changes; before/after and treated/control outcomes define the evaluation contrast; other factors characterize associations rather than automatically establish causes. This is based on Team 2513705, Cracking the Cyber-Puzzle: KDMF in Action, an Outstanding Winner paper with 33 physical PDF pages. Sections 4–6 cover the analyses on pages 6–20; section 7 checks sensitivity on page 21; limitations and memo appear on pages 22–25. The source has inconsistent interpretations of some policy coefficient signs, so the figure must not display an empirical effect direction or claim a positive causal result. Do not print provenance details.

Aesthetic direction: replace the three equal framed columns and generic policy cards with an open evidence diagram built around ONE country-by-year ledger. A small projection of national profiles branches left; a policy-text strand approaches from above; the largest object to the right is a four-cell before/after contrast with symbolic outcomes. A narrower social-factor relation remains below the ledger. These objects should differ in geometry because they perform different operations. A real ledger, a text-to-topic mapping and a difference-of-differences contrast are more informative than shields, offices, folders and random country icons.

Canvas: landscape about 3:2, flat white, comfortable five-percent margins. Put the restrained heading "Cybersecurity policy: assembling the evidence" at x=5%, y=5%. The central evidence ledger occupies x=25–50%, y=33–62%. National profiles use the smaller left area x=5–23%, y=35–69%. The focal policy contrast occupies x=56–95%, y=36–73%. The policy-text strand is a shallow, irregular strip above the ledger and contrast, approximately y=17–29%. A compact socioeconomic analysis sits below the ledger at y=75–87%, and a small synthesis statement sits near the lower-right. Do not enclose these coordinates in colored rectangles.

Identity and color: teal #327F87 identifies the treated group in the comparison, while warm amber #B88745 identifies the control group. These are group-role colors, not judgments of good or bad policy. Neutral charcoal and gray carry country profiles, document traces and covariates. In the clustering projection, use shape distinctions plus faint neutral hulls instead of reusing treated/control colors as cluster assignments. Treatment groups and K-means clusters are not the same thing; do not imply a causal connection between those groupings through shared color.

Evidence ledger: draw a compact table with a Country column, a Year column and a few symbolic measure columns. The Country entries are neutral A, B and C, each repeated across a pre and post row. These are explanatory placeholders, not named countries or actual observations. The Year entries are "pre" and "post", with a small brace above them labeled "country × year". Avoid invented dates and numerical scores. A small header over the measure side reads "Indicators / covariates". Within the cells use short gray dashes and small hollow observation marks rather than unreadable fake decimals.

The indicator labels "NCSI" and "Legal measures" must appear as the two specifically highlighted columns of the ledger. Give their headers a very light neutral underline, not a thick colored band. A small adjacent note lists "Exposure · reporting" as additional recorded indicators, with no arrow implying they are also used for K-means. The source introduces a wider indicator set but clusters using only standardized NCSI and Legal Measures. The figure must make that input selection visible through two short column leaders, not silently send the entire table into clustering.

Show tiny pair braces at the left edge of the repeated pre/post rows so the viewer can recognize the same country across time. One or two matching hollow symbols can recur in the later comparison. Do not populate the ledger with fabricated country data or imply that there are only three countries in the paper. A very small "symbolic records" note below the table can clarify that these rows expose the data structure. This note can be merged into the global schematic note if another local label is too much.

At the upper left of the ledger, a short annotation "aligned definitions / reporting" identifies the data-conditioning step. It is not a new preprocessing module. The underreporting limitation means recorded cybercrime does not equal true incident incidence. Do not draw a complete-data checkmark, a validated shield or a map claiming comprehensive global coverage. A discreet small open gap in one gray table cell can remind the viewer that completeness is not guaranteed, but avoid prominent error symbols that would misrepresent actual missingness counts.

National-profile branch: two thin gray leaders leave only the NCSI and Legal measures columns and converge on a short label "standardize", then "K-means · 3 groups". Draw a small 2-D schematic projection beside it, with axes labeled only "PC1" and "PC2". Arrange three sparse groups of tiny circle, triangle and square marks. Their locations are illustrative, not PCA results from the source data; write "schematic projection" directly below the axes. Do not use precise tick marks, explained-variance percentages, silhouette values or a numeric elbow plot.

The cluster projection should be smaller than the policy contrast and should not dominate the figure with colored bubbles. Three very faint open contours can distinguish the groups, but no opaque circular background enclosing all dots. Keep about four or five representative marks per group, not a dense synthetic scatterplot that looks empirical. Use shape coding deliberately: circle, triangle and square identify profile groups only in this branch. Never carry those group identities into the treated/control rows unless explicitly explained, because they are a different grouping operation.

Below the projection add "national profiles" and a short smaller note "elbow / silhouette checks". This names source diagnostics without claiming a score or that three groups are universally optimal. The output is a descriptive grouping, not a policy treatment assignment. Do not route the clustering output into the LDA text or into the treated/control cells. Its only downstream route is a thin dashed synthesis link to the lower-right conclusion area.

Policy text enters independently along the top: draw two small overlapping document traces, each consisting of five short horizontal lines, with a few highlighted phrase spans. These are stylized text carriers, not fake legal quotations. Next to them write "Policy texts". A light extraction leader moves from the highlighted spans to six concise topic labels set along two short rows, without placing each topic in a pill or card. The labels are "Criminal law", "Data privacy", "Operator duties", "Infrastructure", "Emergency response", and "Cross-border data".

Place "LDA · six themes" above these topic labels in small type. The six topics are a description of document content, not six quantified causal effects. Do not draw word clouds, network-degree scores, topic probabilities or a bar chart of topic weights without data. The phrase highlights use muted gray with a tiny teal edge if needed, but not the full treatment/control palette. The important visual transformation is text spans becoming a named policy theme, not six separate departments connected in a chain.

A thin brace under the six topics leads rightward to the label "policy change". This label ends at a small vertical time marker on the focal before/after contrast. The topic mapping provides policy context; it does not itself generate treatment assignment through a deterministic arrow from topic number to group. Put the short qualification "policy context" on the connecting leader. This stops the diagram from claiming that LDA estimates a causal policy effect.

Focal comparison at x=58–94%, y=37–71%: draw a clean two-by-two arrangement with only thin horizontal and vertical rules. Column headings are "Before" and "After". Row headings are "Treated" and "Control". The row identifier marks are teal for treated and amber for control, matched to small role markers at the corresponding input boundary of the ledger. The role markers are not the cluster shapes and do not refer to actual named countries. Preserve a little more horizontal space between Before and After than between the row labels and their cells.

In the four cells, write exactly the symbolic quantities "Y_T,pre", "Y_T,post", "Y_C,pre", and "Y_C,post", with T/C and pre/post as clear subscripts if possible. These are outcomes, not coefficients or estimated values. Do not print actual incident rates, a sign, a percent reduction or an upward or downward fitted curve. The symbolic contrast is the scientific detail; the viewer should be able to understand the comparison without a decorative trend plot.

Draw one slim horizontal arrow across the treated row from before to after. Above that arrow print "Δ_T". Draw the equivalent arrow across the control row and label it "Δ_C". Both arrows mean a time difference, not improvement, so neither has an upward or downward direction. Their colors follow their row roles. Keep the arrows well away from the symbolic subscripts so they cannot be mistaken for minus signs.

Beneath the four-cell structure, bring two short vertical leaders, one from the treated change and one from the control change, to the single expression "DID = Δ_T − Δ_C". The subtraction sign must be visually distinct and correct. Under it, in smaller text, print "difference of changes". If the cell expressions are legible, the expanded difference formula need not also appear. Avoid doubling the same algebra into a crowded lower panel. The focal contrast should contain no pastel background, thick border, glossy header or three-dimensional chart.

The country-year ledger supplies the outcomes to this focal comparison through one clear neutral connector labeled "matched outcome records". Its arrow ends at the left boundary of the two rows, not at the DID expression itself; this exposes the fact that both groups and both periods are inputs. Treatment/control comparability is a modeling assumption, not something established by the illustration. A small qualifier immediately below the DID expression reads "comparability / reporting limits". There is no green check claiming identification success.

The source uses covariates in the policy evaluation. Represent them as a small shared covariate brace below the ledger, labeled "GDP · connectivity · education". A thin leader from this brace enters the comparison from below, labeled "adjustment". It should not enter only the treated row, which would imply different measurements for controls. Make this connection thinner than the main matched-record path. Do not render regression coefficient estimates or a causal graph with unsupported conditional independencies.

A separate small annotation "legal capacity / cooperation" can join the adjustment brace if space permits. It is supporting context for the evaluation, not a fourth analytic pillar. Do not force every covariate into its own rectangle. A concise label beside a shared brace carries more meaning and leaves space for the focal difference operation. Keep the overall covariate label count within approximately two readable lines, not a paragraph of methodological caveats.

Socioeconomic association branch below the ledger: begin at the covariate brace, not at the DID output. Show a compact three-column symbolic array or strip: neutral attribute marks on the left, an association-strength relation labeled "MIC" in the center, and a smaller joint-model annotation on the right. The attribute labels may be grouped as "income / connectivity / education", reflecting the source's socioeconomic variables. The array must not contain invented coefficients or colored heatmap cells that resemble measurements.

After the MIC relation, draw a slim descriptive continuation labeled "factor analysis + regression". Do not imply that MIC itself proves causal influence or that all source variables were retained with known coefficient signs. The output label is "associated factors". Add "not causal effects" in smaller gray text directly beneath it. This branch can be represented through a narrow mathematical relation rather than another large table, so its visual footprint is different from the focal DID matrix.

Keep the covariate pathways scientifically separate: a shared covariate bundle supports the policy adjustment and also enters the association analysis, but the DID coefficient is not an input to MIC. The national clusters are not automatically the treated/control groups. LDA themes describe the policy documents and do not become numeric incident outcomes. These distinctions should be clear from route endpoints even if a reader skips the small limitation labels.

Synthesis at the lower-right should occupy no more than roughly a tenth of the inked page. Use three short incoming dashed leaders from national profiles, the policy comparison and associated factors. They end beside a small three-line annotation headed "Policy interpretation". The three lines are "profile context", "change comparison", and "associated factors". No boxes, memo-card stack, official seal or recommendation trophy. The labels explain how the evidence is brought together without inventing a ranked policy recommendation.

If the synthesis area needs a visual object, use a single short bracket collecting those three evidence lines, not a policy document with fake prose. The user is interested in the figure, not in a flowchart ending with a generic report. The focal DID operation should remain much larger and more explicit than this final synthesis annotation. Avoid the phrase proven effectiveness, optimal policy, causal discovery or validated impact.

Sensitivity details remain small margin annotations: "DID coefficient perturbation" connects to the comparison's output, and "MIC input-noise check" connects to the MIC relation. They are diagnostics of two different analysis routes; do not put them into one unlabeled loop that suggests retraining every model. No sensitivity curves, numeric error bars, p-values or invented robustness scores. Place these notes near the relevant object instead of a full-width bottom metric strip.

Reporting caveat: recorded indicators can differ from actual cybercrime burden because of underreporting and national measurement differences. This should be acknowledged with one concise "reporting limits" phrase next to the ledger/comparison, not a red warning banner or a defensive paragraph. The source's internal inconsistency in coefficient-sign interpretation is handled by omitting effect directions entirely. The illustration remains informative because it depicts how contrasts are constructed, not because it asserts a favorable result.

Line routing: policy texts and topic labels occupy the top band with short local leaders; their policy-context route lands on the time boundary between Before and After. The central ledger's two highlighted columns connect left to standardization and clustering, using one shared corner rather than crossing the main outcomes route. The outcomes route goes right into the comparison row boundary. Covariate adjustment approaches from below. The MIC branch sits underneath the ledger; its outgoing synthesis route travels through bottom whitespace. The left profile-synthesis connector can run along the lower outer margin, clearly dashed, with no implied input to MIC.

Space and grouping: the profile projection has its own axes but no enclosing card. The ledger has only the rules a table needs. The topic words align loosely with their text extraction braces but do not become a row of six pills. The comparison has a structural matrix, not a decorative frame. The association strip uses a brace and a small relational stroke. These different object silhouettes prevent the page from becoming six identical module boxes while retaining a clear reading path.

Typography: keep the main heading smaller than in the supplied old image. Use no all-caps banner names such as GLOBAL CYBERSECURITY FRAMEWORK. Local labels "National profiles", "Policy change comparison" and "Associated factors" can be medium weight and sit directly beside their objects. Use consistent subscript formatting for Y_T,pre, Y_T,post, Y_C,pre and Y_C,post, and clear Greek delta on both change arrows. If the model cannot render a subscript cleanly, a plain readable Y(T, pre) format is preferable to corrupted notation, but all four cells must use the same convention.

Reference adaptation: borrow Sivia's practice of carrying identifiable evidence through a transformation, and of allocating space to the actual mechanism rather than to every named method equally. Here the persistent evidence is a country observed across time, not a video frame or language-model token. The before/after relation is a symbolic policy contrast, not a benchmark performance chart. Do not copy a multimodal-agent funnel, neural reasoning workspace or reinforcement-learning architecture.

Final review should verify that exactly two source indicators feed K-means; three profile groups are not confused with treated/control; six LDA themes remain descriptive; the four outcomes are in their correct cells; Δ_T and Δ_C are within-group time changes; DID subtracts control change from treated change; covariate adjustment is shared; social associations do not feed the DID coefficient backward; and no numerical effect or sign is asserted. At thumbnail size the eye should see evidence rows, document spans and a large paired contrast, not a row of interchangeable colored modules. Place one small bottom-left note "Schematic · symbolic outcomes, not estimates". Return the finished illustration only.

FINAL REDRAW CORRECTIONS TO THE SUPPLIED FIRST REDESIGN. Preserve the open evidence-led composition, document-to-topic mapping, country-year ledger and symbolic two-by-two comparison. Make these precise repairs.

The national profile scatter currently reuses the same teal circles and amber triangles as the treated/control comparison. This wrongly suggests the profile clusters ARE the policy groups. Recolor ALL cluster points and open contours to grayscale and use three different neutral marker shapes there: small crosses, diamonds and squares. Retain teal circle and amber triangle ONLY for the treated/control rows. Add a small label under the cluster projection "profiles ≠ treatment groups". It replaces any redundant diagnostic text if necessary; do not add another panel.

The two clustering input routes must start at the NCSI and Legal measures column HEADERS, not from the Country column or the general outside table boundary. Route two short thin leaders upward from those two headers, merge above the ledger and go left into "standardize", then K-means. Remove the current second route that bypasses standardization. The Exposure / reporting column must have NO connector to K-means. The ledger itself may be slightly narrower or lower to create a white lane for these leaders.

Remove the long dashed connector that currently has an arrowhead pointing UP into the PC2 axis. National profiles should contribute TO Policy interpretation, not receive input from it. Redraw a single thin dashed route starting below the national-profile projection, traveling across the bottom white margin, and ending with one arrowhead at the synthesis line "profile context". There is no arrowhead at the scatter end. Do not merge that route into MIC or the covariate brace.

Remove the dashed synthesis route that currently starts at the GDP/connectivity/education brace. That brace supplies adjustment and the association analysis, NOT the policy-comparison result. Instead start a short dashed route at the DID expression and end it at the synthesis line "change comparison". Route down through whitespace to the right of the matrix, not through its four symbolic outcomes. The associated-factors output gets its own one-way dashed arrow into the synthesis line "associated factors". Exactly three independent evidence outputs enter the synthesis.

Keep DID coefficient perturbation as a small local annotation adjacent to DID, rather than a long return arrow that could be mistaken for the missing synthesis output. MIC input-noise check stays local to MIC. Keep all four symbolic Y outcomes and DID = Δ_T − Δ_C unchanged. No fitted values or effect signs appear.

Make "policy change" land at the before/after column boundary with a short leader, not at a floating heading. The source policy context does not define the K-means groups, so keep that top route independent from the new grayscale clustering input lane. Keep the main heading restrained and all outer spaces white. Do not add checkmarks, shields, metric strips, cards or a synthetic empirical time-series plot.


## Round 5 — inspected correction

Exact submitted text. ImageGen edit; input: `overview-v4.png`; output: `overview.png`.

RENDERING CONTRACT — read this as drawing instructions, not as text to print.
Recompose the supplied earlier overview completely. It is the previous version of this figure, supplied only to identify the subject being revised. Its repeated cards, thick colored frames, oversized title bands, clip-art fillers and evenly divided dashboard layout are exactly what must change. Preserve the paper-specific scientific facts specified below; do not preserve the old arrangement, old typography, decorative objects, enclosure count or generic icons. Produce one finished landscape scientific illustration, about 3:2, with a white background. Use the largest available native output resolution. This is a manuscript overview at approximately 180 mm print width, not a presentation slide or a website. The illustration must be recognizable through its scientific objects even with the title concealed.

Build visual hierarchy through differences in object size, information density, positioning, negative space and the number of relationships actually drawn. Do not make every analytical activity a similarly sized container. Give the central transformation substantially more area than provenance, diagnostics or administrative outputs. A small contextual drawing may lead into a large mechanism; a thin margin annotation may explain a secondary analysis. Regions described below are coordinates for composing an open page, not instructions to draw region borders. Leave the white page continuous between these regions. Use bounding lines only when they represent an actual matrix, probability space, ledger, coordinate system, document edge or local mathematical grouping.

The main subject should be a data-bearing or state-bearing object, not a floating icon for a department. A table must expose what changes across rows or columns. A trajectory must identify what is evolving. A gate must show its operands and its excluded path. A cash ledger must expose how contributions combine. The shape of an arrow should carry a specific transfer or feedback relation. Keep the same identity marker, symbol and role color whenever the same entity reappears. Do not swap a row into an unrelated decorative object merely to fill a gap. Several carefully differentiated observations are more useful than a wall of anonymous dots or a stock human silhouette.

Draw like a restrained journal illustration with deliberate vector-like edges and small human decisions in the composition. Use flat fills sparingly, never glossy gradients, plastic highlights, beveled cards, drop shadows, glass panels or atmospheric glow. A little low-opacity contextual tint may distinguish a mathematical domain, but it must not become another colored rectangle enclosing a heading and bullets. Do not use a full-canvas colored wash. Do not surround the page with an outline. Avoid circular medallions, numbered badges, rockets, brains, gears, shield mascots and coins unless a specifically justified physical object is requested below. Flatness alone is insufficient: every retained object must make the paper easier to understand.

Use a narrow palette whose roles are stated in the case-specific brief. Neutral charcoal carries text and structural strokes. Secondary material is warm gray. Two restrained accent hues should carry the key distinction, with a third only when a scientific state genuinely needs it. Accents are not category flags assigned independently to every region. A semantic color must travel with the represented entity or operation. Distinguish objects by shape, label, line pattern or position as well as hue. Preserve legibility in grayscale: the active path should not disappear when two colors have similar luminance. White means unfilled space, not an extra category.

Typography is a tool for reading the mechanism. Set one modest, left-aligned figure heading, no more than approximately five percent of canvas height. Do not repeat the manuscript title as a huge poster banner. Use a single clean sans-serif family, regular or medium weight; reserve bold for at most the main heading and the principal operation. Mathematical symbols may use a compatible upright or italic scientific face where needed. Local labels should be short noun phrases immediately beside their objects, not centered in detached ribbons. Keep body labels approximately 8–10 pt equivalent at the intended full-text width. A secondary qualification can be slightly smaller, but not microscopic. If text does not fit, remove redundant words or simplify a glyph rather than reducing all lettering.

Print only the exact visible labels and formula content explicitly specified in the case instructions. The long explanation in this prompt supplies geometry, semantics and constraints; it is not a list of paragraphs to place in the figure. Do not print section names such as rendering contract, object inventory, canvas plan, source authority, audit or reference adaptation. Do not add a textual abstract, methods paragraph, legend for nonexistent quantities, tool branding, a URL, team number, award badge, page number or caption block. One short conceptual-status note is enough for the whole figure, with local qualifications only where interpretation would otherwise change.

Connections must meet actual object edges or named ports. Begin a transfer at the data table, population, state or formula that emits it, and end at the operand or structure that receives it. Do not end arrows in a blank zone below a heading. Route primary arrows along quiet white corridors. Separate an arrow's shaft from a grid rule or coordinate axis. Use modest solid arrowheads for direct data or mathematical flow. Use a thin dashed connector for design guidance, a conditional return or a diagnostic only where the case instructions authorize it. A dashed line is not a universal decorative separator. No ambiguous double-headed arrows, unexplained converging junctions, automatic connections between neighboring regions or hidden reverse dependencies.

Where a single input feeds several analyses, fork once from the output-bearing object and route branches independently. Do not accidentally imply that an upper analysis causes a lower one merely because one is placed above it. Where several quantities combine, bring them to distinct ports of the combining expression, not to a generic box labeled integration. Where a feedback loop is necessary, reserve an outside lane and label the returned state. A return arrow must not pass through an unrelated equation, person or label. Crossings should be avoided; if unavoidable, use a visible bridge rather than an accidental node. Keep arrow labels offset from their shafts and away from endpoints.

Maintain clear separation among an observed measurement, an inferred quantity, a counterfactual comparison and a decision. Solid marks may denote observations; open marks or constrained regions may denote uncertainty when specified. Reconstructed values are not measured totals. A scenario is not a forecast distribution. A symbolic algebraic identity is not a fitted empirical result. Never invent data values, fitted slopes, effect signs, confidence intervals, exact probabilities, dollar amounts or performance gains. Do not draw plausible-looking curves merely to occupy an empty space. When geometry is used schematically, label that fact once and avoid numerical axis ticks that imply calibration.

When a source equation is supplied, preserve its operators, parentheses, signs, subscripts and direction of optimization. Distinguish a minus sign from an arrow. Do not replace a conditional expression with an unconditional claim for visual neatness. Do not introduce a new algorithm name, new optimization criterion, ensemble averaging scheme or causal interpretation. A minor simplifying omission is allowed only where the case brief explicitly delegates it. If many terms are needed, distribute a small number of complete equations near the operations they explain. Never scatter fragments of one expression around the page as decorative mathematics.

The illustration should have multiple scales: a contextual cue, a readable intermediate representation and a precise focal transformation. The context remains subordinate. It should use restrained physical or data detail, not an atmospheric scene. Zoom-like connections can explain how a local observation becomes a mathematical object, but they must identify the same observation. A conceptual inset should share an identity mark with the main object. Do not insert an unrelated schematic simply because the paper mentions another method. Use local whitespace to distinguish context from computation rather than a heavy panel boundary.

Borrow visual principles from the inspected Sivia examples without copying their layouts or subject matter. Useful principles are persistent entity identities, a visible before/after state, a dominant mechanism with asymmetric supporting material, and feedback attached to the state that is actually updated. Do not import the examples' language-model internals, reinforcement learning, benchmark claims, floating metrics or decorative dashboards. Some Sivia examples themselves contain many panels; that is not the principle being requested. The adaptation here is the density of scientific explanation inside a well-composed scene, not the number of rounded rectangles.

Before returning the finished image, examine three reading distances. At thumbnail distance, the main subject and the largest transformation should be evident without reading a title. At normal figure width, a reader should follow the actual question, transformation and output through the objects. At full size, equations, label spelling, branch directions and conditional boundaries must hold up. Remove accidental duplicated headings, repeated generic symbols and connectors whose endpoints have no meaning. Preserve comfortable edge margins and a tight but uncrowded composition. Do not fill remaining whitespace with more text. Return only the finished illustration, not a contact sheet, comparison board, mockup of a journal page, alternative designs or an explanation of the rendering process.

CASE: CRACKING THE CYBER-PUZZLE — a source-grounded ICM 2025 F overview.

Scientific purpose: show how a shared country-level evidence base supports three distinct analyses: national cybersecurity profiles, policy-change comparisons and socioeconomic associations. Policy texts describe what changes; before/after and treated/control outcomes define the evaluation contrast; other factors characterize associations rather than automatically establish causes. This is based on Team 2513705, Cracking the Cyber-Puzzle: KDMF in Action, an Outstanding Winner paper with 33 physical PDF pages. Sections 4–6 cover the analyses on pages 6–20; section 7 checks sensitivity on page 21; limitations and memo appear on pages 22–25. The source has inconsistent interpretations of some policy coefficient signs, so the figure must not display an empirical effect direction or claim a positive causal result. Do not print provenance details.

Aesthetic direction: replace the three equal framed columns and generic policy cards with an open evidence diagram built around ONE country-by-year ledger. A small projection of national profiles branches left; a policy-text strand approaches from above; the largest object to the right is a four-cell before/after contrast with symbolic outcomes. A narrower social-factor relation remains below the ledger. These objects should differ in geometry because they perform different operations. A real ledger, a text-to-topic mapping and a difference-of-differences contrast are more informative than shields, offices, folders and random country icons.

Canvas: landscape about 3:2, flat white, comfortable five-percent margins. Put the restrained heading "Cybersecurity policy: assembling the evidence" at x=5%, y=5%. The central evidence ledger occupies x=25–50%, y=33–62%. National profiles use the smaller left area x=5–23%, y=35–69%. The focal policy contrast occupies x=56–95%, y=36–73%. The policy-text strand is a shallow, irregular strip above the ledger and contrast, approximately y=17–29%. A compact socioeconomic analysis sits below the ledger at y=75–87%, and a small synthesis statement sits near the lower-right. Do not enclose these coordinates in colored rectangles.

Identity and color: teal #327F87 identifies the treated group in the comparison, while warm amber #B88745 identifies the control group. These are group-role colors, not judgments of good or bad policy. Neutral charcoal and gray carry country profiles, document traces and covariates. In the clustering projection, use shape distinctions plus faint neutral hulls instead of reusing treated/control colors as cluster assignments. Treatment groups and K-means clusters are not the same thing; do not imply a causal connection between those groupings through shared color.

Evidence ledger: draw a compact table with a Country column, a Year column and a few symbolic measure columns. The Country entries are neutral A, B and C, each repeated across a pre and post row. These are explanatory placeholders, not named countries or actual observations. The Year entries are "pre" and "post", with a small brace above them labeled "country × year". Avoid invented dates and numerical scores. A small header over the measure side reads "Indicators / covariates". Within the cells use short gray dashes and small hollow observation marks rather than unreadable fake decimals.

The indicator labels "NCSI" and "Legal measures" must appear as the two specifically highlighted columns of the ledger. Give their headers a very light neutral underline, not a thick colored band. A small adjacent note lists "Exposure · reporting" as additional recorded indicators, with no arrow implying they are also used for K-means. The source introduces a wider indicator set but clusters using only standardized NCSI and Legal Measures. The figure must make that input selection visible through two short column leaders, not silently send the entire table into clustering.

Show tiny pair braces at the left edge of the repeated pre/post rows so the viewer can recognize the same country across time. One or two matching hollow symbols can recur in the later comparison. Do not populate the ledger with fabricated country data or imply that there are only three countries in the paper. A very small "symbolic records" note below the table can clarify that these rows expose the data structure. This note can be merged into the global schematic note if another local label is too much.

At the upper left of the ledger, a short annotation "aligned definitions / reporting" identifies the data-conditioning step. It is not a new preprocessing module. The underreporting limitation means recorded cybercrime does not equal true incident incidence. Do not draw a complete-data checkmark, a validated shield or a map claiming comprehensive global coverage. A discreet small open gap in one gray table cell can remind the viewer that completeness is not guaranteed, but avoid prominent error symbols that would misrepresent actual missingness counts.

National-profile branch: two thin gray leaders leave only the NCSI and Legal measures columns and converge on a short label "standardize", then "K-means · 3 groups". Draw a small 2-D schematic projection beside it, with axes labeled only "PC1" and "PC2". Arrange three sparse groups of tiny circle, triangle and square marks. Their locations are illustrative, not PCA results from the source data; write "schematic projection" directly below the axes. Do not use precise tick marks, explained-variance percentages, silhouette values or a numeric elbow plot.

The cluster projection should be smaller than the policy contrast and should not dominate the figure with colored bubbles. Three very faint open contours can distinguish the groups, but no opaque circular background enclosing all dots. Keep about four or five representative marks per group, not a dense synthetic scatterplot that looks empirical. Use shape coding deliberately: circle, triangle and square identify profile groups only in this branch. Never carry those group identities into the treated/control rows unless explicitly explained, because they are a different grouping operation.

Below the projection add "national profiles" and a short smaller note "elbow / silhouette checks". This names source diagnostics without claiming a score or that three groups are universally optimal. The output is a descriptive grouping, not a policy treatment assignment. Do not route the clustering output into the LDA text or into the treated/control cells. Its only downstream route is a thin dashed synthesis link to the lower-right conclusion area.

Policy text enters independently along the top: draw two small overlapping document traces, each consisting of five short horizontal lines, with a few highlighted phrase spans. These are stylized text carriers, not fake legal quotations. Next to them write "Policy texts". A light extraction leader moves from the highlighted spans to six concise topic labels set along two short rows, without placing each topic in a pill or card. The labels are "Criminal law", "Data privacy", "Operator duties", "Infrastructure", "Emergency response", and "Cross-border data".

Place "LDA · six themes" above these topic labels in small type. The six topics are a description of document content, not six quantified causal effects. Do not draw word clouds, network-degree scores, topic probabilities or a bar chart of topic weights without data. The phrase highlights use muted gray with a tiny teal edge if needed, but not the full treatment/control palette. The important visual transformation is text spans becoming a named policy theme, not six separate departments connected in a chain.

A thin brace under the six topics leads rightward to the label "policy change". This label ends at a small vertical time marker on the focal before/after contrast. The topic mapping provides policy context; it does not itself generate treatment assignment through a deterministic arrow from topic number to group. Put the short qualification "policy context" on the connecting leader. This stops the diagram from claiming that LDA estimates a causal policy effect.

Focal comparison at x=58–94%, y=37–71%: draw a clean two-by-two arrangement with only thin horizontal and vertical rules. Column headings are "Before" and "After". Row headings are "Treated" and "Control". The row identifier marks are teal for treated and amber for control, matched to small role markers at the corresponding input boundary of the ledger. The role markers are not the cluster shapes and do not refer to actual named countries. Preserve a little more horizontal space between Before and After than between the row labels and their cells.

In the four cells, write exactly the symbolic quantities "Y_T,pre", "Y_T,post", "Y_C,pre", and "Y_C,post", with T/C and pre/post as clear subscripts if possible. These are outcomes, not coefficients or estimated values. Do not print actual incident rates, a sign, a percent reduction or an upward or downward fitted curve. The symbolic contrast is the scientific detail; the viewer should be able to understand the comparison without a decorative trend plot.

Draw one slim horizontal arrow across the treated row from before to after. Above that arrow print "Δ_T". Draw the equivalent arrow across the control row and label it "Δ_C". Both arrows mean a time difference, not improvement, so neither has an upward or downward direction. Their colors follow their row roles. Keep the arrows well away from the symbolic subscripts so they cannot be mistaken for minus signs.

Beneath the four-cell structure, bring two short vertical leaders, one from the treated change and one from the control change, to the single expression "DID = Δ_T − Δ_C". The subtraction sign must be visually distinct and correct. Under it, in smaller text, print "difference of changes". If the cell expressions are legible, the expanded difference formula need not also appear. Avoid doubling the same algebra into a crowded lower panel. The focal contrast should contain no pastel background, thick border, glossy header or three-dimensional chart.

The country-year ledger supplies the outcomes to this focal comparison through one clear neutral connector labeled "matched outcome records". Its arrow ends at the left boundary of the two rows, not at the DID expression itself; this exposes the fact that both groups and both periods are inputs. Treatment/control comparability is a modeling assumption, not something established by the illustration. A small qualifier immediately below the DID expression reads "comparability / reporting limits". There is no green check claiming identification success.

The source uses covariates in the policy evaluation. Represent them as a small shared covariate brace below the ledger, labeled "GDP · connectivity · education". A thin leader from this brace enters the comparison from below, labeled "adjustment". It should not enter only the treated row, which would imply different measurements for controls. Make this connection thinner than the main matched-record path. Do not render regression coefficient estimates or a causal graph with unsupported conditional independencies.

A separate small annotation "legal capacity / cooperation" can join the adjustment brace if space permits. It is supporting context for the evaluation, not a fourth analytic pillar. Do not force every covariate into its own rectangle. A concise label beside a shared brace carries more meaning and leaves space for the focal difference operation. Keep the overall covariate label count within approximately two readable lines, not a paragraph of methodological caveats.

Socioeconomic association branch below the ledger: begin at the covariate brace, not at the DID output. Show a compact three-column symbolic array or strip: neutral attribute marks on the left, an association-strength relation labeled "MIC" in the center, and a smaller joint-model annotation on the right. The attribute labels may be grouped as "income / connectivity / education", reflecting the source's socioeconomic variables. The array must not contain invented coefficients or colored heatmap cells that resemble measurements.

After the MIC relation, draw a slim descriptive continuation labeled "factor analysis + regression". Do not imply that MIC itself proves causal influence or that all source variables were retained with known coefficient signs. The output label is "associated factors". Add "not causal effects" in smaller gray text directly beneath it. This branch can be represented through a narrow mathematical relation rather than another large table, so its visual footprint is different from the focal DID matrix.

Keep the covariate pathways scientifically separate: a shared covariate bundle supports the policy adjustment and also enters the association analysis, but the DID coefficient is not an input to MIC. The national clusters are not automatically the treated/control groups. LDA themes describe the policy documents and do not become numeric incident outcomes. These distinctions should be clear from route endpoints even if a reader skips the small limitation labels.

Synthesis at the lower-right should occupy no more than roughly a tenth of the inked page. Use three short incoming dashed leaders from national profiles, the policy comparison and associated factors. They end beside a small three-line annotation headed "Policy interpretation". The three lines are "profile context", "change comparison", and "associated factors". No boxes, memo-card stack, official seal or recommendation trophy. The labels explain how the evidence is brought together without inventing a ranked policy recommendation.

If the synthesis area needs a visual object, use a single short bracket collecting those three evidence lines, not a policy document with fake prose. The user is interested in the figure, not in a flowchart ending with a generic report. The focal DID operation should remain much larger and more explicit than this final synthesis annotation. Avoid the phrase proven effectiveness, optimal policy, causal discovery or validated impact.

Sensitivity details remain small margin annotations: "DID coefficient perturbation" connects to the comparison's output, and "MIC input-noise check" connects to the MIC relation. They are diagnostics of two different analysis routes; do not put them into one unlabeled loop that suggests retraining every model. No sensitivity curves, numeric error bars, p-values or invented robustness scores. Place these notes near the relevant object instead of a full-width bottom metric strip.

Reporting caveat: recorded indicators can differ from actual cybercrime burden because of underreporting and national measurement differences. This should be acknowledged with one concise "reporting limits" phrase next to the ledger/comparison, not a red warning banner or a defensive paragraph. The source's internal inconsistency in coefficient-sign interpretation is handled by omitting effect directions entirely. The illustration remains informative because it depicts how contrasts are constructed, not because it asserts a favorable result.

Line routing: policy texts and topic labels occupy the top band with short local leaders; their policy-context route lands on the time boundary between Before and After. The central ledger's two highlighted columns connect left to standardization and clustering, using one shared corner rather than crossing the main outcomes route. The outcomes route goes right into the comparison row boundary. Covariate adjustment approaches from below. The MIC branch sits underneath the ledger; its outgoing synthesis route travels through bottom whitespace. The left profile-synthesis connector can run along the lower outer margin, clearly dashed, with no implied input to MIC.

Space and grouping: the profile projection has its own axes but no enclosing card. The ledger has only the rules a table needs. The topic words align loosely with their text extraction braces but do not become a row of six pills. The comparison has a structural matrix, not a decorative frame. The association strip uses a brace and a small relational stroke. These different object silhouettes prevent the page from becoming six identical module boxes while retaining a clear reading path.

Typography: keep the main heading smaller than in the supplied old image. Use no all-caps banner names such as GLOBAL CYBERSECURITY FRAMEWORK. Local labels "National profiles", "Policy change comparison" and "Associated factors" can be medium weight and sit directly beside their objects. Use consistent subscript formatting for Y_T,pre, Y_T,post, Y_C,pre and Y_C,post, and clear Greek delta on both change arrows. If the model cannot render a subscript cleanly, a plain readable Y(T, pre) format is preferable to corrupted notation, but all four cells must use the same convention.

Reference adaptation: borrow Sivia's practice of carrying identifiable evidence through a transformation, and of allocating space to the actual mechanism rather than to every named method equally. Here the persistent evidence is a country observed across time, not a video frame or language-model token. The before/after relation is a symbolic policy contrast, not a benchmark performance chart. Do not copy a multimodal-agent funnel, neural reasoning workspace or reinforcement-learning architecture.

Final review should verify that exactly two source indicators feed K-means; three profile groups are not confused with treated/control; six LDA themes remain descriptive; the four outcomes are in their correct cells; Δ_T and Δ_C are within-group time changes; DID subtracts control change from treated change; covariate adjustment is shared; social associations do not feed the DID coefficient backward; and no numerical effect or sign is asserted. At thumbnail size the eye should see evidence rows, document spans and a large paired contrast, not a row of interchangeable colored modules. Place one small bottom-left note "Schematic · symbolic outcomes, not estimates". Return the finished illustration only.

FINAL SIMPLIFICATION OF THE SUPPLIED SECOND REDESIGN. Keep the grayscale cluster projection, symbolic ledger, six topic names, four correct DID outcomes and the open white layout. Remove the ambiguous long connections; use local input labels where that is clearer.

DELETE every connector above the ledger running from standardize toward the Country-by-year heading or DOWN into the NCSI and Legal measures headers. These arrows currently point backward. Instead, directly above the national-profile projection, write the compact source label "NCSI + Legal measures", then a short downward arrow to "standardize", then a short downward arrow to "K-means · 3 groups". The source label explicitly refers to the two identically named table columns. Do not draw any long line from that local input label back to the ledger. Do not include Exposure / reporting in that label. The existing grayscale projection and "profiles ≠ treatment groups" statement remain unchanged.

DELETE the ENTIRE long dashed connector along the left and bottom margins. In particular, there must be NO arrowhead entering PC2. DELETE all dashed lines from the associated-factors output into the policy-interpretation brace, since the current two-pronged routing targets the wrong evidence labels. DELETE the long DID-to-interpretation connector as well. Synthesis needs no crossing arrows. It is enough to put a restrained brace under the final annotation with three local source names: "national profiles", "policy contrast", "associated factors". Keep "Policy interpretation" as its modest heading. The matching names explicitly identify its three inputs without claiming a false direction of influence. No replacement outer loops or newly inferred connections.

DID coefficient perturbation must be a small plain gray local annotation immediately beside the DID expression, with no long dashed return arrow. MIC input-noise check must likewise be a small local note under MIC, with no arrowhead intruding into the formula. These are diagnostics, not new evidence outputs.

Move the end of the policy-context leader to the vertical Before/After boundary, making one clear downward pointer. Do not end it on a floating heading. If a line to the boundary would collide with Before or After, keep only the short text "policy change" above the boundary and omit the redundant pointer. Keep the ledger-to-matched-records arrow and the shared-covariate adjustment route unchanged. The comparison still has exactly four symbolic outcome cells and DID = Δ_T − Δ_C, with no estimated effect sign.

This edit deliberately replaces confusing long routes with unambiguous source labels and local equations; do not compensate by adding panels, new branch lines, synthetic metrics or more prose. No blue/orange points in the cluster projection. No causal claim for MIC. The final image should be quieter and scientifically clearer than the supplied version.
